스테이징 준비됐습니다. 오늘부터 연동 개발 시작하셔도 됩니다.

*접속 정보*
• 주소: `https://cb-x-deck-staging.vercel.app`
• API 키: DM으로 따로 보내드립니다 (`Authorization: Bearer <키>`). 채널·문서엔 올리지 말아 주세요.
• 명세: 첨부 `settlement-external-api.md` 갱신본(2차 회신 때 보낸 것과 동일)

*데이터 — 한 가지 정정*
회신에 "가짜 데이터"라고 했는데, 내부 개발이라 **실제 슬랙 결제 요청 8/24~8/27 42건을 원본 그대로** 넣었습니다(클리닉 5·인플루언서 33). 인플루언서 실제 계좌·PayPal 정보가 포함돼 있으니 **스테이징이라도 외부 공유·캡처 금지**로 다뤄 주세요. 그쪽 운영 방침(별도 DB·암호화)은 스테이징 미러에도 그대로 적용해 주시면 좋겠습니다.
• 전부 `status: "requested"`, `settlement.status: null` 상태입니다 — 상태 POST를 보내면 우리 화면 배지가 바뀌는 걸 이쪽에서도 확인합니다.
• 이관 데이터라 `requester.email`·`requester.slack_id`는 **전부 `null`**입니다(요청자에 멤버 계정 없음). 운영에서는 `email`이 항상 있으니, 이 두 필드 매핑 검증은 운영 전환 후에 해 주세요.
• 데이터가 어지러워지면 말씀 주세요 — 원래 상태로 리셋해 드립니다(1분).
• 가져간 뒤 `paid`를 찍은 건은 우리 쪽에서도 되돌릴 수 없으니(운영과 동일 규칙), 잠금 동작 테스트는 한두 건으로 해 주시면 리셋 없이 계속 쓸 수 있습니다.

*처음 해 보시면 좋은 순서*
1. `GET /api/external/settlement/requests?limit=100` (cursor 없이) → 42건, `next_cursor` 저장
2. 같은 커서로 재호출 → 빈 목록(`has_more: false`)
3. 아무 건에 `POST …/{request_id}/status` `{ "status": "received", "updated_at": "<지금 ISO>" }` → 200 `applied: true`
4. **30초 이상 지난 뒤** 저장한 커서로 다시 GET → 그 건이 `settlement.status: "received"`로 다시 내려오는지(에코) 확인. 30초 안에는 안전 지연 때문에 아직 안 보이는 게 정상입니다.

*운영 전환*
스테이징에서 다 되면 주소를 `https://cb-x-deck.vercel.app`으로, 키를 운영 키(별도 전달)로 바꾸면 끝입니다. 코드는 동일합니다. 단, **운영 주소에는 이 API가 아직 배포되지 않아 지금 호출하면 404가 납니다** — 배포되면 알려 드리겠습니다.

문의는 이 채널/DM으로 주세요.
