# 캠페인 v2 작업 패널 UI 개편 — 설계

- 날짜: 2026-09-23 · 브랜치 `cb-koo/task-panel-ui`(origin/main `ef48c02`에서)
- 대상: `/campaigns/flow`의 작업 패널(`src/app/campaigns/flow/TaskPanel.tsx`) — **기존 작업 편집·새 작업 만들기 두 모드 공통**
- 시안: `.superpowers/brainstorm/…/content/` panel-v1·v2·v3, influencer-v1 (로컬 전용)

## 1. 왜

koo(09-23) 지적 네 가지 + 대화 중 추가 세 가지:

1. 단계·유형이 헤더의 회색 글자(`준비 · 투고`)라 눈에 안 들어온다 → 태그로.
2. 칸(필드) 사이 구분이 약해 한 덩어리로 보인다 → 칸마다 시각적 구분.
3. 인플루언서가 `@핸들` 글자뿐이다 → 원형 프로필 사진 + 이름 + 핸들.
4. 원고 칸이 글자 링크(`AI로 만들기 · 직접 쓰기 · 있는 원고 고르기`)라 눈에 안 들어온다.
5. 비용 칸에 이 작업의 결제 수단을 보여주고, 수단이 2개 이상이면 고를 수 있게, 없으면 새로 등록할 수 있게.
6. 수수료 부담 유형(인플 부담 / CB 부담)도 함께.
7. 명부에 없는 인플은 배정할 수 없게 하고, 그 자리에서 명부에 등록하는 흐름으로.

## 2. 단계 나눔 (koo 결정: 같은 브랜치, 두 단계, 머지는 한 번)

| 단계 | 내용 | 서버·DB |
|---|---|---|
| **1단계 — 보이는 것** | §3 헤더 · §4 섹션 상자 · §5 단계·유형 칸 · §6 인플 표시 · §7 원고 칸 · §7-1 인용 대상 카드 · §8-1 결제 수단·수수료 **표시** · §8-4 게시 칸 · §10 문구 정리 | 조회만 늘림(마이그레이션 없음) · 게시물 조회 창구 1개 추가 |
| **2단계 — 고르고 등록** | §8-2 작업별 수단 **선택** · §8-3 수단 **신규 등록** · §9 명부 게이팅 + 등록하고 배정 | 마이그레이션 060(칸 추가 1개) · 정산 후보 계산 변경 |

1단계는 혼자서도 머지 가능한 상태로 끝낸다(2단계가 늦어지면 1단계만 먼저 내보낼 수 있게).

## 3. 헤더 (모드별)

시안 2의 B1(헤더 태그 유지)/B2(본문으로 이동) 중 **B2 채택, 단 사진은 뺀다** — 사진은 인플루언서 칸에 한 번만 나온다(중복 제거).

| 모드 | 윗줄(작게) | 제목(20px) |
|---|---|---|
| 기존 작업 | — (단계·유형은 본문 첫 칸으로) | `@핸들` / 미배정이면 회색 `인플루언서 미정` |
| 새 작업 | — | `새 투고 작업` / 유형 전이면 `어떤 작업인가요?` (지금과 같음) |
| 원고 모드 | `← 작업으로` (지금과 같음) | `원고 · 투고 · @핸들` (지금과 같음) |

헤더 오른쪽(`···` 메뉴·`✕`)은 그대로.

## 4. 섹션 상자 (시안 B)

- 본문 바탕을 연회색(`x-surface` 계열, `#f7f9f9`)으로, 칸마다 흰 상자(테두리 `x-border`, 모서리 12px, 안쪽 여백 14×16px, 상자 사이 10px).
- 칸 제목은 상자 안 왼쪽 위 14px·semibold·`x-secondary`. 도움말은 13px `x-muted`.
- 칸 순서는 지금처럼 `PANEL_FIELD_ORDER`(campaignFlowView) 하나만 따른다. 상자는 렌더 래퍼 하나(`PanelSection`)로 만들어 edit/new가 같이 쓴다 — 두 모드가 각자 상자를 그리면 한쪽만 모양이 어긋난다.
- 취소된 작업 안내(`취소된 작업이에요 — …`)는 상자 목록 맨 위 별도 띠로 유지.
- 밀도 기준(koo 반복 피드백 — 도구 화면은 여유 있게): 본문 15px(`text-content`), 보조 13px 이상, 상자 안 한 줄 정보 6개 이하. 패널 폭 560px 그대로.

## 5. 단계·유형 칸 (본문 첫 상자)

- **기존 작업:** 두 줄 격자.
  - `단계` — 흐름 줄 `준비 › 전달 › 게시 › 정산 › 완료`, 현재 단계만 검은 칩(흰 글자), 나머지는 회색 칩. 칩 글자 **14px**(13px 하한 위반 피함). 값은 `flowStage()` 그대로(새 판정 없음).
  - `유형` — `TASK_TYPE_LABEL` 칩(투고·인용RT·RT·방문협찬), 색은 FlowTable의 `TYPE_CHIP`과 같은 값. 두 파일이 같은 표를 복사해 들고 있으므로 이번에 `campaignFlowView`(또는 공용 모듈)로 옮겨 표·패널이 하나를 쓴다. `STAGE_CHIP`도 같이 옮긴다.
  - 취소된 작업: 흐름 줄 대신 회색 `취소` 칩 하나.
  - 둘 다 읽기 전용 — 입력칸처럼 보이는 테두리·호버를 주지 않는다(거짓 어포던스 금지).
- **새 작업:** 이 상자가 지금의 `유형` 칸(세그먼트 버튼 / 원고가 붙으면 잠긴 칩 + "원고를 떼면 바꿀 수 있어요")이 된다. 단계 줄은 없다(만들기 전이라).

## 6. 인플루언서 칸

- 배정됨: `[원형 사진 36px] 표시 이름(15px semibold) / @핸들(13px x-secondary)` + 오른쪽 `[바꾸기]`(지금 버튼 그대로, 비활성 이유 문구도 그대로).
  - 사진 없음(명부 160명 중 14명): 이니셜 원. 인플 명부 화면의 `Avatar`(`src/app/influencers/InfluencerProfile.tsx:18`)를 `src/components/Avatar.tsx`로 옮겨 공용으로 쓴다.
  - 표시 이름이 없으면 `@핸들` 한 줄만.
  - 지금 있는 동작은 그대로 옮긴다: `[해제]`(TaskPanel:357), 게시 후엔 버튼 없이 표시만, 미배정인데 게시 확인된 작업에 배정할 때의 '한 번 적으면 바꿀 수 없어요' 확인 — 게이팅된 '등록하고 배정'도 이 확인을 거친다.
- 미배정·입력 중: 지금의 `InfluencerField` 그대로(2단계에서 게이팅 추가, §9).
- 새 작업에서 원고가 붙어 잠긴 상태: 잠긴 칩 대신 같은 사진+이름 카드를 회색으로 + "원고를 떼면 바꿀 수 있어요".
- 데이터: `listOptions`(`influencerStore.ts:397`, `/api/drafts/influencers`)에 **`avatarUrl`만** 추가 → `InfluencerOption.avatarUrl?`. 결제 수단은 넣지 않는다(§8 데이터 참고).

## 7. 원고 칸

**붙어 있을 때** — 작은 카드(테두리 상자 안의 상자, 모서리 10px):
- 1줄: 원고 제목(`draftLabel`, 15px semibold) + 상태 배지(`STATUS_LABEL`).
- 2줄: 본문 첫 포스트 앞부분 2줄 말줄임(14px `x-secondary`).
- 오른쪽: 첫 이미지 썸네일 52px(없으면 자리 없음). 원고 이미지는 비공개 버킷의 **저장소 경로**라(`draftMedia.ts:124`) 그대로 `<img src>`에 못 쓴다 — `useSignedMedia`(FlowDetail의 `proofUrls`와 같은 방식)로 서명 URL을 받아 쓴다.
- 아래: `열기` · `떼기` (지금과 같은 동작).
- 새 작업(폼에서 고른 원고)도 같은 카드 + 도움말 "만들기를 누르면 이 원고가 함께 붙어요"(지금 문구).
- 데이터: `campaignTaskStore.ts:120`의 `draft_first_line`은 이미 첫 포스트 전문이다(첫 줄 자르기는 `firstLineOf`). FlowRow에 `draftPreview`(첫 포스트 전문, 표시 쪽에서 2줄 clamp)와 `draftFirstImage`(`coalesce(d.edited, d.content)->'posts'->0->'media'->0->>'url'`)를 추가한다. 표의 원고 열(`draftFirstLine`)은 그대로. 새 작업 쪽은 이미 `DraftRow` 전체를 갖고 있어 조회 추가 없음.

**비어 있을 때** — 버튼 세 개(3열 격자, 테두리 상자 버튼):
- `AI로 만들기`(파란 테두리·연파랑 바탕으로 강조) / `직접 쓰기` / `있는 원고 N`(pickCount, 못 읽었으면 숫자 생략). 보조 글·도움말은 두지 않는다(§10).
- 409 경고(`다른 작업에 붙었어요 — 다시 고르기`)는 지금 자리·톤 그대로(오류 문구는 줄이지 않는다).
- 취소된 작업: 지금처럼 글자만(`원고 있었음: …` / `—`).

### 7-1. 인용 대상 카드 (koo 결정: 시안 target-v1의 B)

- **인용RT + 원고가 붙어 있음:** 원고 카드 안을 X 게시 미리보기로 — 인플 사진(§3의 '사진 한 번'의 예외 — 실제 게시 모습이라 koo 09-23 유지 결정)·이름·핸들 → 원고 본문(2줄) → 그 아래 **인용 카드**(대상 게시물의 작성자 사진·이름·@핸들·날짜·본문 2줄). X에 올라갈 모습 그대로. 이때 `인용RT 대상` 칸은 한 줄(`이 캠페인의 @핸들 투고` / 외부 링크면 `@작성자 게시물`) + `[바꾸기]`만.
- **인용RT인데 원고 없음 / RT:** 인용 카드를 `대상` 칸 안에 보여준다(RT는 원고 칸이 없으므로 항상 이 모양). 본문 3줄 + 첫 이미지.
- 카드 컴포넌트: 보관함의 `QuotedCard`(`src/components/QuotedCard.tsx`)를 재사용 — 새로 만들지 않는다. 카드를 누르면 원문이 새 탭으로(QuotedCard 기존 동작). 필요한 손질: `DeckTweet` → `{id, text, userName, screenName, enriched}` 변환, 줄 수 prop(2·3줄 — 지금은 collapsible만 2줄이고 그땐 클릭이 펼치기로 바뀐다), 첫 이미지만 보이는 prop, 바깥 여백(`mt-3` 고정) prop.
- 상태: 대상 작업이 아직 게시 전(또는 게시 확인됐지만 링크 없음 — 판정은 `postedAt`이 아니라 `postUrl`, campaignTaskStore:36) → 점선 상자 "게시되면 여기에 보여요" / 대상 작업이 취소됨 → 회색 "대상 작업이 취소됐어요 — 바꾸기" / 대상이 다른 캠페인 작업이면 한 줄에 캠페인 이름(`targetLabel` sub) / 불러오는 중 → "게시물 불러오는 중…" / 삭제·비공개 → "게시물을 불러올 수 없어요" + 원래 링크 / 대상 미정 → 지금의 대상 입력(TargetLinkField·TargetPicker).
- 데이터: 대상 URL은 `targetUrlOf()`(작업 대상이면 그 작업의 `postUrl`, 아니면 `targetTweetUrl`)로 정한다. 새 조회 창구 `GET /api/tweets/by-link?url=` — 트윗 캐시(`getTweetsByIds`)를 먼저 보고, 없거나 본문이 비었을 때만 `getTweetDetail` 1회 후 `upsertTweets`(generate.ts `loadQuoteTarget`과 같은 규칙, 보관함 항목은 만들지 않는다). 그래서 X 조회는 게시물당 처음 한 번뿐. 새 작업(로컬 target)도 같은 창구를 쓴다. 게시물 id는 **링크 그대로** 쓴다(원고 생성의 `loadQuoteTarget`이 id가 다르면 거부하므로, 미리보기가 원본을 보여주면 생성만 실패하는 어긋남이 생긴다). 순수 리포스트 링크면 "리포스트 링크예요 — 원본 게시물 링크로 바꿔 주세요". 삭제·비공개는 캐시에 안 남아 열 때마다 X를 다시 부르므로, 화면(FlowDetail)이 URL별 결과를 세션 동안 기억해 같은 패널 재오픈에 다시 부르지 않는다. `loadQuoteTarget`의 조회·캐시 부분은 공용 함수로 떼어 두 곳이 같이 쓴다.

## 8. 비용 · 정산 칸

칸 제목을 `비용 · 정산`으로(방문협찬은 `예산 · 정산` — 지금 라벨 `예산`을 따른다). 안은 두 줄 격자.

- `금액` — 지금의 `CostConfirmField`(edit: `slots.cost`, new: 직접 렌더) 그대로.
- **새 작업의 금액 저장 방식 변경(koo 09-23 결정, 1단계):** 지금은 [확인]을 눌러야만 `newCost`가 잡혀, 프로필 단가가 칸에 보이는데도 [만들기]만 누르면 비용 없이 만들어진다(보이는 값 ≠ 저장되는 값). 바꾼다 — [만들기]·[만들고 하나 더]는 **칸에 보이는 금액·통화**를 함께 보낸다(`CostConfirmField`가 현재 입력값을 부모에 올린다).
  - 칸이 비어 있으면 비용 없이 만든다(지금과 같음). 숫자로 읽을 수 없는 값이면 만들지 않고 금액 칸에 `AMOUNT_MESSAGE`.
  - 프로필 단가와 다른 금액(또는 프로필에 단가 없음)으로 만들면 **만들기가 성공한 뒤 한 번** 지금의 `PriceProfileDialog`("프로필에도 반영할까요?")를 띄운다(koo 09-23 결정). 판정은 지금 `CostConfirmField.confirm()`의 시나리오 그대로 — 명부 밖·통화 불일치면 묻지 않는다. [만들고 하나 더]도 같다(폼은 비운 뒤 물음).
  - 새 작업에서는 금액 칸의 [확인] 버튼과 점선(미확정) 표시를 없앤다 — [만들기]가 곧 확정이라 두 번 확정하는 셈이 된다. 기존 작업 편집은 칸마다 바로 저장하는 모드라 지금처럼 [확인] 유지.
  - 쓰던 방식이 바뀌므로 `updates.ts`에 적는다.
  - 인플 목록(`influencerOptions`)이 늦게 도착하면 금액 칸이 빈 채로 마운트돼(프로필 단가는 마운트 때 한 번만 채운다) '보이는 값 저장'이 비용 없이 저장된다 → 목록 도착 후 다시 채워지게(마운트 key에 목록 도착 여부 포함). 금액 오류 문구는 [만들기]에서도 띄울 수 있게 `error` prop을 연다.
- `결제 수단` — 아래 §8-1~3.

### 8-1. 표시 (1단계)

- 값: 이 작업에 쓰일 수단 = (2단계 이후) 작업이 고른 수단, 없으면 인플의 기본 수단. 문구는 `describeMethod()`, 수수료는 `formatFee()`/`settlementBadge` 계열과 같은 말(koo 09-25: `수수료 부담 없음` / `수수료 3% 부담` / `수수료 300엔 부담` — 우리가 부담하는지 여부) — 인플 프로필과 다른 말을 쓰지 않는다.
- 수수료 칩: 인플 부담 = 회색, **CB 부담 = 주황**(비용이 늘어나는 쪽).
- 상태별:
  - 수단 1개: 글자로만(드롭다운 없음).
  - 수단 없음(명부 38명): 주황 "결제 수단이 없어요 — 정산 요청 전에 필요해요" (1단계는 안내만, 2단계에서 [+ 결제 수단 등록] 버튼).
  - 인플 미배정: 칸 자체를 회색 "인플을 정하면 보여요".
  - 정산 요청을 보낸 뒤(단계 `정산`·`완료`): 요청 스냅샷의 수단을 보여주고 고정. 도움말 "요청에 담긴 수단이에요 — 바꾸려면 정산 화면에서 요청을 취소해요".
  - 명부에 없는 핸들: 결제 수단 칸에서는 다루지 않는다(§9에서 인플 칸이 처리). 칸은 "인플을 명부에 등록하면 보여요".
- **데이터:** 결제 수단(계좌번호 포함)을 `listOptions`에 싣지 않는다 — 그 응답은 원고 생성 화면도 매번 받는다. 패널이 열릴 때 **새 가벼운 조회 `GET /api/influencers/payment-view?handle=&taskId=`** 하나로 받는다 — 활성 정산 요청이 있으면 그 스냅샷(설명 + 수수료), 없으면 명부 인플의 기본 수단(설명 + 수수료), 명부 밖·수단 없음은 상태만. 새 작업은 handle만 보낸다. 화면이 (핸들·작업·요청 상태)별로 캐시한다. 2단계의 수단 목록(드롭다운)은 2단계 계획에서 이 조회를 넓혀 싣는다. `GET /api/influencers/[id]`는 로그·원고 50건·분석까지 6개 쿼리라 패널마다 부르기엔 무겁다. 권한은 `listOptions`와 같은 `requireAllowedUser`. 정산 요청 후 스냅샷은 FlowRow에 싣지 않는다 — `settlementByTaskIds`는 캠페인 목록의 모든 행에 붙어 나가므로(campaignStore:285) 계좌번호·이메일이 전 작업분 새어 나간다. 요청 뒤 상태는 위 조회가 그 작업의 요청 한 건에서 설명 문자열 + `payment_request.fee`만 돌려준다(스냅샷엔 fee·id가 없다, `toMethodSnapshot`). '요청 후' 판정은 단계 판정과 같은 조건(`status = 'requested'`이고 `externalStatus ≠ 'cancelled'`, 즉 단계 `정산`·`완료`).

### 8-2. 작업별 선택 (2단계, koo 결정: "이 작업만")

- 수단 2개 이상(명부 10명): 드롭다운. 항목 = `✓ PayPal · sakura@… · 기본 · 인플 부담`, 맨 아래 `+ 새 결제 수단 등록`. 도움말 "바꾸면 이 작업의 정산에만 적용돼요. 인플 프로필의 기본 수단은 그대로예요".
- 저장: **마이그레이션 060** — `alter table campaign_task add column if not exists payment_method_id text` (추가만, 1회 머지, AGENTS.md 규칙). 값은 인플 `payment_methods[].id` **참조**(스냅샷 아님 — 스냅샷은 정산 요청 때 이미 뜬다). null = 기본 수단.
- edit 모드: 고르는 즉시 PATCH(다른 칸과 같은 낙관적 갱신). new 모드: 로컬로 들고 있다가 [만들기]에 함께.
- 인플을 바꾸면 `payment_method_id`를 null로 되돌린다 — 다른 사람의 수단 id가 남으면 안 된다(서버에서 보장): `updateTask`에서 `influencerHandle`이 **대소문자 말고 실제로** 바뀔 때, `replaceInfluencer`(campaignTaskStore:500). `renameInfluencer`(influencerStore:322)는 같은 사람이라 유지 — 그래서 DB 트리거로 하지 않는다.
- 서버 검증: PATCH·생성 모두 id가 배정된 인플의 현재 `payment_methods[]`에 있는지 확인(없으면 400). **활성 정산 요청이 있으면 `paymentMethodId` 변경을 서버가 거부**한다 — `reviseRequest`(settlementStore:563)가 후보를 다시 계산하므로 UI 잠금만으로는 다음 수정 때 스냅샷이 조용히 바뀐다.
- 배관: `parseTaskPatch`(campaignTaskInput:164) · `TaskPatch`/`updateTask` · `TaskCreateInput.items`/`createTasks` · `buildTaskCreateBody` · Row/SELECT · `CandRow`/`CANDIDATE_BASE`(settlementStore:57-77). `taskPaymentMethod`는 `rowToCandidate` 안에서 불러 생성·수정(revise)·`expected` 대조를 한 번에 덮는다. `campaignStore:136`은 클라이언트 예산 패널의 수수료 합계(`feeKrw`)에도 쓰인다.
- **정산 후보 계산:** `settlementStore.ts:102`와 `campaignStore.ts:136`(캠페인 수수료 합계)이 `getDefaultPaymentMethod` 대신 공용 함수 `taskPaymentMethod(methods, task.paymentMethodId)`를 쓴다 — 고른 id가 목록에 있으면 그것, 없으면 기본. `settlementStore.ts:237`의 `expected.paymentMethodId` 대조는 그대로 동작(같은 id 체계).
- **고른 수단이 나중에 삭제되면:** 기본 수단으로 돌아가고, 패널이 주황으로 "고른 수단이 삭제돼 기본 수단으로 정산돼요"를 보여준다. 정산 후보 화면에는 이번에 표시하지 않는다(백로그).
- 정산 요청 후엔 선택 불가(§8-1 고정 상태).

### 8-3. 신규 등록 (2단계)

- 입구: 수단 없음 상태의 `[+ 결제 수단 등록]`, 수단 1개일 때 줄 오른쪽 보조 버튼 `다른 수단 등록`([바꾸기]와 같은 모양, koo 09-25), 드롭다운 맨 아래 항목.
- 폼: 인플 프로필의 `MethodForm`(`src/app/influencers/PaymentSection.tsx:331`)을 팝업(다이얼로그)에 띄운다 — 폼을 새로 짓지 않는다. 지금은 비공개이고 입력 상태·검증·`send()`가 `PaymentSection`(80-130) 안에 있어, 이 셋을 함께 떼어 공용으로 만든다(프로필 화면도 같은 것을 쓰게). 새로 생긴 수단 id는 응답 배열을 등록 전과 비교해 찾는다. 저장은 기존 `POST /api/influencers/[id]/payment-methods`. 등록 결과는 인플 프로필에도 그대로 남는다(같은 데이터).
- 첫 수단이면 자동 기본(기존 규칙). 두 번째 이상을 등록하면 **이 작업의 선택으로 바로 잡는다**(방금 이 작업 때문에 등록했을 것이므로).
- 패널 위에 뜬 다이얼로그 동안은 패널 Esc를 끈다(`overlayOpen` 관례).

### 8-4. 게시 칸 (기존 작업만)

- 지금 `slots.posted`(FlowDetail) 그대로, 맨 아래 상자에 넣는다: 게시 전 `[게시 확인]` / 게시 후 `게시 9월 24일 · 게시물 보기 ↗` + `내림 표시` / RT는 `증빙 보기`·`증빙 없음` / 인플 미배정이면 비활성 + "인플 선택 후". 새 작업에는 없다(지금과 같음).
- 유형별 칸 순서는 `PANEL_FIELD_ORDER` 그대로(투고: 인플→비용→원고→게시 예정일→메모, 인용RT: …원고→대상→…, RT: 원고 없음, 방문협찬: 일정→예산→원고), 그 뒤에 게시 칸.

## 9. 명부 게이팅 + 등록하고 배정 (2단계) — koo 09-24 개정: 화면 + 서버 둘 다

**원칙:** 사람이 입력하는 모든 인플 칸은 "명부에서 고르기 / 명부에 등록하고 배정"만 된다. 서버도 명부 밖 핸들을 새로 저장하지 않는다. 이미 들어와 있는 명부 밖 핸들은 건드리지 않는다(새로 넣거나 바꿀 때만 막는다). 근거: koo는 앞으로 작업을 슬랙이 아니라 프로덕트에서 직접 입력한다(09-24) — 9월 4주차 `coco`·`kero`·`ngm`(앞부분만 친 핸들)이 화면 입력으로 들어온 실제 사고. 기존 `/campaigns` 화면은 제거 예정이라 입력 경로가 아니다.

**입구(코드 조사 09-24, 기존 캠페인 화면 제외):**
- 캠페인 v2: ① 새 작업 인플 칸(`createTasks`) ② 미배정 작업 배정(`updateTask`) ③ `[바꾸기]`·행 메뉴 교체(`ReplaceDialog` → `replaceInfluencer`) ④ 원고 모드 원고 카드의 인플 칩(`InfluencerChip`, FlowDetail `onAssignInfluencer`)
- 콘텐츠 생성(`/generate`): ⑤ 원고 카드 인플 칩(같은 `InfluencerChip`) — 여기 적은 핸들이 (a) 원고 카드 '작업' 칸의 새 작업 만들기(`DraftTaskField` → `createTasks`) (b) 캠페인 v2 '있는 원고 고르기'로 붙일 때 미배정 작업에 채워짐(`attachDraft`의 coalesce, campaignTaskStore:222)으로 작업에 넘어간다.
- 관계없음: 명부 이름 변경(`renameInfluencer`, 명부 기준), 연습용 DB 시드, 039 이관(1회), 정산 외부 API(작업 인플을 바꾸지 않음).

**화면:**
- `InfluencerField`를 목록을 직접 그리는 콤보박스로 다시 만든다(지금은 `<datalist>`라 사진·이름·단가 행과 '등록하고 배정' 행을 못 그린다). `role="listbox"`(패널 바깥 클릭 예외 TaskPanel:255가 이 역할로 판정). 게이팅은 `rosterOnly` prop — ①~⑤ 전부 켠다. 다른 화면(링크 만들기 등 작업 인플이 아닌 곳)은 지금 동작 그대로.
- 확정 로직이 있는 곳 전부에 게이팅: TaskPanel `commitNewHandle`·`commitEditHandle`, `ReplaceDialog.resolveHandle`, `InfluencerChip.save`. 명부에 있는 핸들은 blur로도 확정(지금과 같음).
- `influencerOptions`를 못 읽었으면(실패 시 빈 배열) 모든 핸들이 '명부 밖'으로 보인다 → 이때는 "명부를 불러오지 못했어요 — 새로고침해 주세요" 한 줄 + 배정 막음(서버가 어차피 거부하므로 자유 입력으로 열어 두면 저장 단계에서 실패한다).
- 동작(시안 influencer-v1):
  1. 입력하면 명부 후보(사진·이름·핸들·단가).
  2. 명부에 없는 핸들이면 후보 자리에 `@핸들 명부에 등록하고 배정` 한 줄만(§10). Enter/blur로는 배정하지 않는다(X 조회 비용 → 누르는 opt-in, UX 원칙 6).
  3. 누르면 그 줄에 `불러오는 중…`, `POST /api/influencers` → 성공(`created:true` 또는 대소문자만 다른 기존 행 `created:false`도 성공) → `influencerOptions` 다시 읽기 → 배정.
  4. 실패: X에 없는 계정 → 서버 문구(`api/influencers:48`) 그대로 빨강; 조회 실패(502) → 서버 문구(`LOOKUP_FAILED`) 그대로 + 다시 시도 가능. 둘 다 배정하지 않는다.
- **이미 명부 밖으로 배정된 작업(현재 9월 2주차 5건):** 인플 칸에 이니셜 원 + `@핸들` + `명부에 없음` + `[명부에 등록]`(3과 같은 호출, 배정은 이미 돼 있으니 등록만). 자동 정리는 하지 않는다.
- 원고 쪽: ⑤의 칩이 막히므로 새 원고에는 명부 밖 핸들이 안 생긴다. 예전 원고에 남은 명부 밖 핸들은 원고 카드에 같은 `명부에 없음` 표시.

**서버:**
- 공용 판정(`src/lib/taskAssignGate.ts`, 명부에 있으면 명부 표기 핸들, 없으면 거부) — **저장 라우트 5곳**(작업 POST·PATCH·교체, 원고 단건·일괄 PATCH)이 부르고 400 "명부에 없는 인플이에요 — 명부에 먼저 등록해 주세요"를 돌려준다. 저장소 함수(`createTasks` 등)에는 넣지 않는다 — 테스트·시드 130여 곳이 임의 핸들로 직접 부르고, 사람이 입력하는 길은 전부 이 라우트를 거친다(2단계 계획 '설계와 다르게 정한 것' 1).
- 원고 인플 배정(원고 PATCH의 `influencerHandle`, 수동 원고 생성)도 같은 판정 — ⑤의 서버 쪽.
- `attachDraft`: 작업이 미배정이고 원고 핸들이 명부 밖이면 작업을 채우지 않고 미배정으로 둔다(붙이기는 성공). 명부 안이면 지금처럼 채운다.
- 대소문자만 다른 핸들은 같은 사람으로 본다(`lower(handle)` 비교, 지금 관례). 저장 표기는 명부 표기로 맞춘다.
- 기존 데이터는 바꾸지 않는다 — 판정은 "새로 넣거나 바꿀 때"만. 이미 명부 밖인 작업의 메모·비용·일정 PATCH는 막지 않는다(influencerHandle을 안 건드리면 판정 안 함).

## 10. 문구 정리 (koo 09-23: "설명이 장황해 패널이 복잡하다")

규칙: ① 평소(정상) 상태엔 도움말 없음 ② 막혔거나 주의할 때만 짧게 한 줄 ③ 더 긴 이유는 ⓘ(마우스 올리면 title/툴팁). 오류 문구(무엇을 하면 되는지 알려주는 것)는 줄이지 않는다.

| 자리 | 지금 / 이전 시안 | 바꿈 |
|---|---|---|
| 결제 수단(평소) | 바꾸면 이 작업의 정산에만 적용돼요… | 소제목 옆 `· 이 작업에만 적용` |
| 결제 수단 없음 | 결제 수단이 없어요 — 정산 요청 전에 필요해요 | `결제 수단 없음` + `[+ 등록]`(2단계) |
| 고른 수단 삭제됨 | 고른 수단이 삭제돼 기본 수단으로 정산돼요 | `기본 수단으로 바뀜` ⓘ |
| 정산 요청 뒤 | 요청에 담긴 수단이에요 — 바꾸려면… | `🔒 정산 요청됨` ⓘ |
| 지급 완료 뒤(정산 프로덕트가 지급 완료를 보냄, 단계 `완료`) | (없음 — '정산 요청됨'으로 나왔다) | `🔒 지급 완료` ⓘ (koo 09-24) |
| 인플 미배정(비용·수단·게시) | 인플을 정하면 프로필 단가로 채워요 / 인플을 정하면 보여요 / 인플루언서를 먼저 정해요 | `인플 선택 후` (비용·정산 상자에 한 번만) |
| 비용 확정 전 | 프로필 단가 · 인용RT | `프로필 단가` |
| 비용 프로필과 다름 | ✓ 확정 · 프로필 단가 40,000원과 다름 | `✓ 확정 · 프로필 40,000원` |
| 원고 비어 있음 | 버튼 보조 글 + 인플루언서가 직접 쓰면 비워 둬요 | 버튼 세 개만 |
| 게시 확인 | (도움말 제안) | 버튼만 |
| 명부에 없는 핸들 | X에서 프로필을 불러와요 + 명부에 없는 인플은 배정할 수 없어요… | `@핸들 명부에 등록하고 배정` 한 줄만 |
| 예전 명부 밖 배정 | 명부에 없는 인플이에요 — 등록하면… | `명부에 없음` + `[명부에 등록]` |
| 원고 붙어 잠긴 인플·유형 | 원고를 떼면 바꿀 수 있어요 | 🔒 ⓘ |
| 방문일 라벨 | 방문일 — 지나면 인플루언서를 바꿀 수 없어요 | `방문일` (ⓘ로 이유) |
| 새 작업 하단 | 비어 있는 칸은 나중에 채워도 돼요 / 유형을 먼저 골라요 | 앞은 없앰 · 뒤(막힌 이유)는 유지 (koo 09-24) |
| 새 작업 금액(직접 고쳤을 때) | (없음 — [확인]이 없어 저장되는지 헷갈림) | `만들기를 누르면 저장돼요` (koo 09-24) |
| 새 작업에서 고른 원고 | 만들기를 누르면 이 원고가 함께 붙어요 | `만들기를 누르면 함께 붙어요` (koo 09-24) |

위 §5~§9 본문의 문구가 이 표와 다르면 이 표가 우선이다.

## 11. 범위 밖 (백로그)

- 정산 후보 화면에 "고른 수단 삭제됨" 표시.
- 9월 2주차 명부 밖 5건 정리(koo 09-24: 일단 둔다).
- 기존 `/campaigns` 화면(TaskAddModal)은 제거 예정이라 게이팅하지 않는다.

## 12. 검증

- 기타: `TYPE_CHIP`은 4벌(FlowTable·TaskTable·WeekCalendar·TargetPicker)이라 공용화 때 모두 옮긴다 · `Avatar` 이동 시 `influencers/page.tsx:12` import 갱신, 이미지 로드 실패(`onError`) 시 이니셜로 · FlowRow 픽스처는 `campaignFlowView.test.ts`·`campaignTaskCancel.test.ts` 둘.
- 순수 로직 테스트(tsx 단일 파일): `taskPaymentMethod`(고른 id 있음/삭제됨/null), 칩 색 표 공용화 후 표·패널 동일, FlowRow 새 필드 매핑(`campaignFlowView.test.ts`의 `mk()` 픽스처 갱신).
- DB 통합 테스트(연습용 DB): 060 적용 후 정산 후보가 고른 수단을 쓰는지, 인플 변경 시 `payment_method_id`가 비는지.
- 전체 `npm test`(연습용 DB) + 타입 검사 + 린트 기준선(24개) 유지.
- 화면 확인: 컴포넌트 하네스가 없어 koo가 직접 본다 — 기본은 **프리뷰 배포**(koo 선호). 프리뷰 URL의 Supabase OAuth 허용 목록(와일드카드) 설정이 아직 미확인이라, 막히면 `npm run build && npx next start -p 3001` + `http://127.0.0.1:3001`.
- 머지 직전 `src/content/updates.ts`에 한 건(유형 `개선`, 쓰던 방식이 바뀐 것 — "명부에 없는 인플은 패널에서 바로 배정되지 않고 등록을 거쳐요" — 반드시 적는다).
