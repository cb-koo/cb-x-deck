# 캠페인 v2 (워크플로 화면) — 결정 기록

작성: 2026-09-15 · 갱신: 2026-09-18 · 브랜치 `cb-koo/campaign-workflow-v2` · 상태: **화면 전부 시안 검토로 확정(09-17~18, 원고 모드 포함). 다음 = 구현 계획**
방식: mattpocock `grilling` → `domain-modeling`(용어집 `CONTEXT.md`, ADR 0001~0006) → 이 문서 → 정적 HTML 시안(`~/claude-outputs/20260915_캠페인v2_시안.html`, 변형 F) → 확정 → 구현 계획
근거: koo 손그림(2026-09-14, 캠페인 생성 → 정산 요청 8단계 + 유형별 작업 추가 순서) · 옛 시도(2026-09-03 스펙)는 **전제로 삼지 않음**
용어는 `CONTEXT.md`를 따른다. 각 항목의 표기: **확정** / **가안**(시안 검증) / **별도 스펙**.

## 0. 문제와 방향

- **문제**: 8단계에 필요한 기능(칸)은 다 있다. 그런데 캠페인 하나를 실제로 처리하면 ① 만들고 → ② 작업 넣고 → (DM 조율) → ⑤⑥ 확인 → ⑧ 정산까지 가는 동안 **화면이 손을 끊는다.** 지금 프로덕트는 단계의 결과값을 적는 장부이고, 그림은 단계를 밟는 절차다.
- **방향**: **한 화면에서 전체 플로우를 처리하는 UX.**

## 1. 그림 ↔ 프로덕트 대응

| 그림 | 지금 | v2에서 |
|---|---|---|
| ① 캠페인 생성 | 있음 | 기존 부품 재사용 |
| ② 작업 추가 · 유형 선택 | 있음(순서는 한 틀) | 팝업, 유형별 순서(§4) |
| ③ 인플 요청/조율 (원고 전달·비용) | 없음 — DM | 저장 안 함(ADR 0001) |
| ④ 진행/거부 · drop or change | 없음 | 취소·교체(ADR 0002·0005) |
| ⑤ 예정일 확인 | 있음 | 진행 중 행의 칸(섹션 아님, §3) |
| ⑥ 게시 확인 | 있음 | 수동만 이전, 자동 조회는 추후 |
| ⑦ 완료/미완료 처리 | 없음 | 게시 확인 = 완료, 취소 = 미완료로 덮음 |
| ⑧ 정산 요청 | 정산 페이지 | 정산 페이지에 남김 + 캠페인 딥링크(ADR 0004) |

## 2. 뿌리 결정 (전부 확정)

| # | 결정 | 근거 |
|---|---|---|
| R1 | 화면 단위 = **캠페인 하나** (좌측 목록 + 상세) | ①이 캠페인 생성, 정산도 캠페인(주차) 단위로 나감 |
| R2 | **새 페이지** `/campaigns/flow`, 사이드바 "캠페인" 아래 **"캠페인 v2"**. 자리 잡으면 기존 대체 | ADR 0003 |
| R3 | ⑧은 **정산 페이지가 담당**, v2는 정산 대기 건수 + 캠페인 미리 고른 링크 | ADR 0004 |
| R4 | 조율 상태 저장 안 함 — **진행이 기본값, 예외(취소·교체)만 입력** | ADR 0001 |
| R5 | 작업 추가는 **팝업 → 유형 선택 → 유형별 입력** | 그림 그대로 |
| R6 | **손대지 않음**: 인플 명부·결제 수단·트래킹(화면·연동)·콘텐츠 성과·리포트·정산 그쪽 연동·`/generate`(안정화 후 결정). **예외**: 트래킹 스토어가 **취소 작업으로의 게시물 연결을 거절**(기존 `rt-task` 거절과 같은 자리 — 연결 저장 전에 던져 트랜잭션 전체가 롤백, 보충만 건너뛰지 않음)(R17), 정산 페이지 필터 초기값 파라미터(R15) | 그림 밖, 읽기만 |
| R7 | 원고 상태(초안~전달됨·미사용)는 **단계가 아니라 행의 부가 정보** | RT(절반)엔 원고가 없어 단계로 두면 유형별로 갈라짐 |
| R8 | 교체 = **같은 행에서 인플만 바꿈**, 작업 ID 유지. 게시 전만, 방문협찬은 **방문일이 지나기 전까지(당일 허용, `visit_on < 오늘`이면 불가, 미정이면 허용)**. **인플 칸을 바꾸는 모든 요청(배정·해제·교체)에 같은 상태 제한**. 해제는 옛 사람 흔적 정리(전달됨→사용 확정·증빙 제거), 배정은 비용 있으면 묻기 — 그래야 "해제→재배정"으로 교체를 우회 못 함. 교체 = 해제+배정 한 트랜잭션 + 사유. 같은 인플 재선택은 no-op. 서버 액션 라우트에서 `for update` 재검사 + 인플·비용·원고 상태·증빙·로그를 한 트랜잭션. 비용이 있으면 **항상 묻기**(유지 / 새 단가). 원고 '전달됨'→'사용 확정' 되돌림. RT 증빙 제거. 사유 선택 → 옛 인플 타임라인 로그 | ADR 0005 |
| R9 | 취소 = 상태·사유 선택·되돌리기·원고 자동 떼기(무엇이었는지 기억)·집계 제외·게시 전만 | ADR 0002 |
| R10 | 요약 카드 4·주간 달력·인플별 비용 표는 **가져오지 않음** | 무엇이 그리웠는지 보고 대체 시점에 |
| R11 | 게시 확인은 **수동 등록만**(게시됨 표시·게시물 링크·RT 증빙·내림). 리포스터 자동 조회는 추후 | 정산 계약 유지 |
| R12 | 칸 편집(예정일·비용·대상·인플·게시 확인·증빙)·캠페인 목록·생성·헤더는 **기존 부품 그대로**. 인플 교체는 공용 훅 한 동작으로 두 화면이 공유 | ADR 0003 |
| R13 | 원고 쓰기 입구는 **패널의 원고 칸**(AI로 만들기 / 직접 쓰기 / 있는 원고 고르기) — 같은 패널이 원고 모드로 바뀐다(§5) | 원고는 작업에 붙는 것 |
| R14 | 원고 모드(생성·직접 쓰기·고르기)는 **§5로 확정** — 별도 스펙 불필요. 현 `/generate`의 불편은 레퍼런스 고르기와 화면 왕복이었고(koo 09-18) 패널이 둘을 줄인다 | koo 09-18 |
| R15 | 정산 페이지 검토 대기 탭에 `?tab=candidates&campaign=` 초기 필터 1개. 0건 처리·탭 전환 시 제거 | ADR 0004 |
| R16 | 데이터 변경은 마이그레이션 **055** 하나 — `campaign_task`에 `cancelled_at date` · `cancel_reason text check in ('declined','no_response','other')` · `cancel_note text not null default ''` · `cancelled_draft_id uuid references draft on delete set null` · `cancelled_draft_title text` · **`check (cancelled_at is null or posted_at is null)`** + `influencer_log` 이벤트 `task_declined` | ADR 0002·0005, 새 테이블 없음 |
| R17 | **집계에서 빼는 조건은 취소만.** 기존 "미사용 원고 작업 제외"(`isTaskUnused`) 규칙 폐지 — 원고 미사용은 원고의 상태, 작업은 진행 중·비용 포함. 판정 함수·집계 SQL(`taskStage`·`summarizeTasks`·`subtotalsByType`·`deriveTaskInfluencers`·`campaignStore` task_count/totals)이 공통 `isTaskExcluded` 사용. 게시 확인 경로(`updateTask`·`markPosted`)에 `cancelled_at is null` 가드, `trackingStore.linkTrackedPost`는 취소 작업 연결 자체를 거절 | ADR 0002·0003, koo 09-15 |
| R18 | 취소 중 허용: 메모·되돌리기·명시적 삭제·캠페인 삭제 cascade. 금지(400): 인플·비용·예정일·방문일·대상·원고 붙이기·게시 확인·증빙 | ADR 0002, koo 09-15 |
| R19 | 취소된 작업이 다른 RT·인용RT의 대상이면 `targetStatus='cancelled'`("대상 작업 취소됨") + 대상 바꾸기. 새 대상 후보·"이미 RT하기로 한 사람"에서 제외. 하위 연쇄 취소 없음. 대상 복원 시 하위는 파생값이라 자동 복귀 | ADR 0002 |
| R20 | 기존 캠페인 화면도 취소를 보인다 — 표에 흐린 글씨 + '취소됨' 칩, 편집 불가. 조작은 v2에서 | ADR 0003 |
| R21 | 작업의 위치는 **6단계로 파생**(저장 안 함): 준비 → 전달 → 게시 → 정산 → 완료 / 취소. 규칙 §3-1 | koo 09-17 ("진행 중/완료/취소 3자리"를 대체) |
| R22 | 작업은 **유형별 개수로 뼈대를 먼저** 만들고 빈 작업에 인플·비용·원고·대상·예정일을 채운다. 입구 둘 — `+ 작업 추가`(패널 새 작업, [만들기]로 생성) · `한 번에 만들기`(유형별 개수 창) | koo 09-18 — 실제 계획 순서 |
| R23 | **행 클릭 = 오른쪽 편집 패널**(폭 560). 이전/다음으로 연속 채움. 읽기 전용 행 펼침은 두지 않는다 | ADR 0006 |
| R24 | 비용은 인플 배정 시 프로필 단가로 채우되 **[확인]을 눌러야 확정**. 같음→확정 / 다름→프로필도 바꿀지 묻기 / 단가 없음→입력 후 프로필에 저장할지 묻기. 확정 전엔 점선·표에서 회색 | koo 09-18 |
| R25 | RT·인용RT 대상은 **링크 입력으로 통일**. "아직 게시 전인 글" 체크 → (선택) 어느 작업인지 → 그 작업이 게시 확인되면 링크 자동 채움(내부만 `target_task_id`) | koo 09-18, 스키마 변경 없음 |
| R26 | 표 규칙: 열 6개(단계·유형·인플·원고 첫 줄·게시 예정일·비용) + `···` · 행 한 줄 · 굵은 글씨·버튼 없음 · 빈 값은 **미정** · 기본 정렬 만든 순, 헤더 클릭 오름/내림/기본 · 필터 드롭다운(단계·유형·지금 볼 것, 중복 선택) · 검색 한 칸(인플·원고 제목·첫 줄) | koo 09-17 |
| R27 | 날짜 칸 색만으로 구분: 빨강 `M/D 요일 · D+N`(지남) / 검정(예정) / 파랑(게시일). 행 배경·막대·'오늘' 강조·'게시' 문구·증빙 표시 없음 | koo 09-17 |
| R28 | 타이틀 아래 요약 카드 3장 **작업 · 성과 · 비용**(§3-3). 유형별 건수 카드는 두지 않음 | koo 09-17~18 |

## 3. 화면 (확정 — 시안 F, 09-17~18)

좌측 캠페인 목록(접기 가능, 기존 `listCollapsed` 재사용) + 상세. 상세 = 타이틀 · 요약 카드 3장 · 필터/검색 줄 · **표 하나**.

### 3-1. 단계 (R21) — 파생 규칙

| 단계 | 조건 (위에서부터 먼저 맞는 것) |
|---|---|
| 취소 | `cancelled_at` 있음 |
| 완료 | 정산 요청의 그쪽 상태 = 지급 완료 |
| 정산 | 활성 정산 요청 있음(요청됨·접수·지급 예정·보류) |
| 게시 | `posted_at` 있음 (= 정산 대기) |
| 준비 | 인플 미배정, 또는 원고가 붙는 유형(투고·인용RT·방문협찬)인데 원고가 아직 없음, 또는 원고가 있고 상태가 전달됨·미사용이 아님 |
| 전달 | 그 외 — 원고 전달됨, 또는 RT처럼 원고가 없는 유형에 인플이 있음 |

정산에서 요청이 취소되면 다시 '게시'로 돌아간다(파생값). 단계 칩 색: 준비 회색 · 전달 파랑 · 게시 녹색 · 정산 보라 · 완료 검정 · 취소 흐림+취소선.

### 3-2. 표 (R26·R27)

```
[필터 ▾] [⌕ 인플루언서 · 원고 검색]  전체 13건 · 만든 순              정산 대기 2건 · 정산에서 확인 →
단계   유형    인플루언서      원고                              게시 예정일        작업 비용
전달   투고   @toppogi0102   ホワイトニングして3日目。正直…       9/15 화 · D+1     ₩80,000  ···
전달   RT     @kawachi_x     —                                 9/17 목           ₩30,000  ···
준비   투고   미정            スケーリングって半年に…             미정              ₩60,000  ···
게시   인용RT @rararanll     日本語で予約できるって…             9/15 화(파랑)     ₩30,000  ···
정산   인용RT @skysky_ca     施術の後に写真もらえた…             9/14 월(파랑)     ₩100,000 ···
완료   투고   @17dsy_        スケーリング後3日目。…              9/14 월(파랑)     ₩60,000  ···
취소   인용RT @ririko_item   🙅 거절 · 일정 안 맞음 · 원고 있었음: … 9/15 화 취소   ₩60,000  ···
```

- **원고 칸 = 본문 첫 줄**(제목·상태 문구 없음). 넘치면 말줄임, 전체는 툴팁. RT는 `—`. 원고 없는 투고류도 `미정`.
- **대상·방문일·증빙·메모는 표에 없다** — 패널(§4)에서 본다.
- `···` = 게시 확인 · 예정일 바꾸기 · 원고 열기(또는 붙이기/새로 만들기) · 인플루언서 교체 · 작업 취소 · 삭제. 취소 행은 되돌리기 · 삭제.
- 필터 드롭다운: 단계 6 / 유형 4 / 지금 볼 것(지연·오늘 게시 예정·예정일 미정). 묶음 안 OR, 묶음 사이 AND. 요약 줄에 고른 것과 건수, [지우기].
- 검색: 인플 핸들(@ 유무 무관) + 원고 제목 + 첫 줄. 서버 목록이 이미 `draft_title`·`draft_first_line`을 실어 보내므로 화면에서 걸러도 된다.
- 정렬: 기본 만든 순(먼저 만든 것 위). 헤더 클릭 → 오름 → 내림 → 기본. 미정은 오름차순에서 맨 뒤.
- 하단 한 줄: 유형별 건수 · 비용 합 · 게시 n/N · 밀림.
- 인플 핸들은 링크가 아니다(koo 09-17 취소).

### 3-3. 요약 카드 (R28) — 제목 한 단어 · 큰 숫자 하나 · 라벨은 숫자 아래 · 주석은 툴팁 · 기호는 `/` 하나

```
작업               성과              [업데이트]    비용
4 / 12             70,200   839   133            ₩250,000 / ₩810,000        ₩3,000,000
게시               조회     좋아요  북마크         소진 / 계획                  9월 예산
▬▬▬░░░░░                                          ▒▒▒▒▒▬▬▬░░░░░░░░░░░░░░
```

- **작업**: 게시 확인된 작업 / 계획된 작업(취소 제외) + 막대.
- **성과**: 게시물 링크가 등록된 완료 작업의 최신 스냅샷 합(`tracked_post`의 views·likes·bookmarks). 숫자는 천 단위 구분만(만 단위로 접지 않음). **[업데이트]** = 링크 있는 작업의 게시물을 다시 조회해 스냅샷 추가(기존 트래킹 수집기 재사용, 게시물당 API 1회). 툴팁에 기준 시각·합계 밖 건수.
- **비용**: **소진** = 게시 확인된 작업 비용 합 / **계획** = 취소 제외 작업 비용 합 + 인플별 추가 비용(현행 월 집계와 같은 정의 — 추가 비용은 작업별이 아니라 소진엔 넣지 않음, 툴팁에 "추가 비용 ₩n은 계획에 포함"). 오른쪽에 클라이언트 **예산**(1차는 현행 월 예산 · "9월 예산", 예산 기간 모델 뒤엔 "9/1–9/28 예산"). 막대 = 예산이 전체 길이: 회색(같은 달/기간의 다른 캠페인 **계획**) → 진한 파랑(이 캠페인 소진) → 연한 파랑(이 캠페인 계획 잔여) → 빈칸(남음). 송금 수수료는 미포함(토글 없음, 툴팁). 카드는 `campaignMonthBudget()` 한 함수로 예산을 받으므로 달→기간 전환 시 카드 코드는 그대로.
- 비율 0.9 : 1.1 : 1.4. 유형별 건수 카드는 뺐다(유형 열·필터로 충분).

## 4. 작업 만들기·채우기 = 오른쪽 패널 (확정, R22~R25)

### 4-1. 입구 둘

- **`+ 작업 추가`** → 패널이 "새 작업"으로 열림. 유형 선택 → 칸들 → **[만들기]** 또는 **[만들고 하나 더]**. 유형만 골라도 만들기 전엔 표에 생기지 않는다(koo 09-18).
- **`한 번에 만들기`** → 작은 창 `인용RT [5] 투고 [2] RT [3] 방문협찬 [0]` → **[작업 10개 만들기]** → 빈 작업(단계 준비·전부 미정)이 생기고 첫 작업 패널이 열린다.
- 옛 작업 추가 팝업(한 작업의 모든 칸을 한 번에)은 **폐기**.

### 4-2. 패널 (폭 560)

```
준비 · 투고                                   ···  ✕
@asyako0520
─────────────────────────────────────────────
인플루언서   [A @asyako0520] [바꾸기]
비용         [₩80,000 (점선)]  [확인]   프로필 단가 · 투고
원고         [AI로 만들기] [직접 쓰기] [있는 원고 고르기 4]   ← 붙으면 제목 · 상태 + [열기] [떼기]
대상 게시글   [https://x.com/…]                              ← 인용RT·RT만
             ☐ 아직 게시 전인 글이에요 — 링크는 나중에
                └ (체크 시) 어느 작업인가요? [@toppogi0102 투고 ▾]  선택 안 해도 됨
게시 예정일   [미정]
메모
─────────────────────────────────────────────
                               ← 이전   8 / 13   다음 →
```

- **칸 순서**(koo 09-18): 인용RT·투고·RT = 인플 → 비용 → 원고(RT 없음) → 대상(투고 없음) → 예정일 → 메모. 방문협찬 = 인플 → 일정(방문일·게시 예정일) → 예산 → 원고 → 메모.
- **이전/다음**은 표 순서(만든 순). 패널에서 고치면 표가 바로 바뀐다.
- `···`(취소·교체·삭제)는 패널 헤더 오른쪽. 새 작업(만들기 전)엔 없음.
- 패널이 열려 있는 동안 표는 어둡게(스크림), 행 클릭으로 다른 작업 전환.

### 4-3. 비용 [확인] (R24)

| 시나리오 | 흐름 |
|---|---|
| 4-1 프로필 단가 있음 · 같음 | 자동 채움 → `프로필 단가 · 투고` → [확인] → ✓ 확정 |
| 4-2 프로필 단가 있음 · 다름 | 자동 채움 → 사용자가 고침 → `프로필 단가 ₩30,000과 다름` → [확인] → 창 "프로필의 인용RT 단가도 ₩30,000 → ₩35,000으로 바꿀까요?" [이 작업만] [프로필도 바꾸기] → 확정 |
| 4-3 프로필 단가 없음 | 빈칸 `프로필에 단가 없음 — 직접 입력` → 입력 → [확인] → 창 "프로필에 투고 단가 ₩50,000으로 저장할까요?" [이 작업만] [프로필에 저장] → 확정 |

- 확정 전: 입력칸 점선, 표의 비용 회색(툴팁 "아직 확인 전"). 확정 후 `✓ 확정`(프로필 갱신했으면 `· 프로필 단가 갱신됨`).
- 프로필 갱신은 기존 `influencer.pricing` + `pricing_changed` 로그를 쓴다. 인플 교체 때의 비용도 같은 [확인] 흐름(ADR 0005 갱신).
- 인플 미정이면 비용 칸 비활성("인플을 정하면 프로필 단가로 채워요").

### 4-4. 대상 (R25)

- 입력은 **링크 하나**. 체크 "아직 게시 전인 글이에요"만 하면 표·패널에 `게시 확인 전` 표시, 그 캠페인의 게시 확인 다이얼로그가 "대상 미정인 RT 작업 n건 — 이 게시물로 채울까요?"를 제안.
- 어느 작업인지까지 고르면(선택) 내부적으로 `target_task_id`를 두고, 그 작업이 게시 확인되는 순간 링크(`post_url`)를 자동으로 채운다 + 알림. 화면은 여전히 링크 하나.

### 4-5. 게시 확인 다이얼로그

`···` 또는 패널에서: 언제(오늘 / 다른 날) · 게시물 링크(선택, 정산 참고자료) · RT면 증빙 스크린샷 **필수** · "확인하면 완료로 내려가고, 비용·인플이 있으면 정산 대기에 잡혀요. 되돌리지 않아요." 기존 `PostedCell` 선택지와 같다. 리포스터 자동 조회는 이 화면에 없음(R11).

## 5. 원고 모드 (확정, 09-18 — koo가 시안에서 1~6단계 확인 · 구현 완료 C 계획, 2026-09-19)

패널의 원고 칸에서 **[AI로 만들기] / [직접 쓰기] / [있는 원고 고르기 n]** 을 누르면 **같은 패널이 원고 모드**로 바뀐다(위에 `← 작업으로`). 패널과 시트는 별개 표면이 아니다. 클라이언트·인플·유형·대상은 작업에서 와서 잠김. 인플 미정 작업에서도 열린다.

### 5-1. 생성 (첫 화면 = 매번 바꾸는 둘만)

> **1.5 예고**: 레퍼런스 고르기([+ 보관함에서] 뒤의 창)는 1차에서 기존 `RefPickerSheet`를 그대로 붙이고, **v2 1차 배포 직후 별건으로 재설계**한다(koo 09-18: 보관함 콘텐츠를 확인·선택하는 UX가 지금 설계에 없음 — 카드 미리보기·좁히기·선택 상태·비교). 패널은 껍데기라 알맹이 교체가 쉽게 만든다.

```
레퍼런스   [🔗 대상 · @toppogi0102 투고]  [+ 보관함에서] [+ 링크]    ← 인용RT는 대상 게시물 자동 포함
방향성     [                                                 ]
▸ 설정     시술 치아미백 · 단문 · 시안 1개 · 제약 켬 — 지난번 그대로  ← 접힘. 시술·형식·제약만 클라이언트별로 기억
[시안 1개 만들기]
```
- 레퍼런스 출처는 지금과 같이 **보관함 고르기 + 링크 붙이기**. 인용RT의 대상 게시물만 자동. **성과 기반 자동 제안은 넣지 않는다**(koo 09-18: 지금 단계에 필수 아님, 복잡해질 걱정).
- 시안 수 기본 **1**, 1~5(koo 09-19 확정 — 아래 주석). 만드는 동안 패널에 머무른다(진행 표시). 뒤에서 돌리고 다른 작업 채우기는 실사용 뒤 검토.
  - 09-18 시점 결정은 기본 3이었다. 구현 중 시안 수를 클라이언트별로 기억하면 한 번 5로 둔 사람이 그 뒤 모든 생성에서 조용히 5배를 내는 것이 드러났고(`/generate`가 같은 이유로 저장에서 빼고 생성 뒤 1로 되돌린다), 기억을 없애면서 시작값도 `/generate`와 같은 1이 됐다. koo가 1로 확정했다 — 여러 개가 필요하면 `▸ 설정`에서 올린다. 비용 문구가 `× 시안 수`로 함께 말한다.
- 결과 = 시안 카드 3장, **읽기 + [이 시안 붙이기]만**. 다듬기는 붙인 뒤.
- 붙이면 그 자리가 **원고 카드(`DraftCard`)** 로 바뀐다 — 다시쓰기·번역·이미지·버전·복사·상태(초안→검수 대기→사용 확정→전달됨)·[떼기]. `DraftCard`는 `max-w-[600px]`·내부 고정 폭 없음이라 패널 안 516px에 그대로 들어간다(`/generate`·기존 캠페인 상세에 이어 세 번째 자리).
- 안 고른 시안은 **작업 없는 원고로 남는다**(형제 시안). 서버는 "생성은 미부착, 선택 시 부착" — 기존 `createDraft({taskId})`(첫 시안 자동 부착)와 다른 새 경로.

### 5-2. 직접 쓰기 = X 포스트 작성 UI (koo 09-18)

X 컴포저와 같은 모양으로 쓴다 — 결과물이 X에 올라갈 글이니 쓰는 화면이 같으면 글자 수·줄바꿈·스레드 감이 그대로 잡힌다(덱 = X 미러링 원칙과 같은 결).

- **가져오는 것**: 아바타·핸들(작업의 인플루언서 — 누구 이름으로 나가는 글인지) · 글자 수 원형 카운터(X 규칙: 일본어 등 전각 140자 가중치, 초과 시 빨강) · **스레드**(아래 `+`로 다음 글, 글마다 카운터, 연결선) · **이미지 첨부**(글 아래 미리보기 격자 최대 4장, 기존 Storage).
- **안 가져오는 것**: 이모지 피커·투표·GIF·예약.
- 버튼은 **[저장하고 붙이기]** — 저장 즉시 이 작업에 붙고 원고 카드로 바뀐다. 제목(선택)은 카드에서.
- 저장 구조는 지금 원고 모델 그대로(본문 배열 = 단문/스레드, 이미지 = 기존 첨부). 서버 변경 없음.
- **원고 카드 편집의 컴포저 통일은 1차 구현(2026-09-19)에서 제외**했다 — `DraftEditModal`(평범한 텍스트 칸)은 그대로 있다. `/generate`·기존 캠페인 상세까지 파급되는 별건이라 §8로 옮긴다(koo 09-18: 통일이 맞되 크기 보고).

### 5-3. 있는 원고 고르기
두 묶음 — **같은 재료로 만든 시안**(이 캠페인·같은 클라에서 생성됐으나 안 붙은 형제 시안) / **작업 없는 원고**(그 외 같은 클라이언트). 검색 한 칸이 둘 다에 걸림. 다른 클라이언트 원고는 대상 아님.

### 5-4. 작업 모드의 원고 칸
- 원고 없음: 세 버튼. RT는 원고 칸 없음. 인플이 직접 쓰면 비워 둔다.
- 원고 있음: `제목 · 상태 [열기] [떼기]`. [열기] → 원고 모드의 카드. 표의 원고 열은 본문 첫 줄.

## 6. 취소·교체 세부 (확정 — ADR 0002·0005 요약)

- **취소**: 게시 전만. `where posted_at is null and cancelled_at is null` 조건부 갱신 + DB check로 게시 확인과 상호 배제. 취소 컬럼·원고 떼기·스냅샷·로그는 **한 트랜잭션**. 원고는 떼고 `cancelled_draft_id`·제목 스냅샷 보존. 제외되는 비용은 작업 비용만, 인플별 추가 비용은 남음.
- **되돌리기**: 외부 트랜잭션 하나 — ① 작업 복원 UPDATE(취소 컬럼·스냅샷 전부 지움) → ② 세이브포인트 안에서 재부착 → ③ 한 번 커밋. 중간 커밋 없음. unique 충돌(다른 작업이 그 사이 붙임)이면 세이브포인트만 롤백하고 "원고는 @d 작업으로 갔어요". 3갈래(재부착 / 다른 작업에 갔음 / 원고 없음) — 원고 점유·삭제가 작업 복원을 실패시키지 않는다는 뜻에서 "항상 성공".
- **서버 계약**: 취소·되돌리기·교체는 PATCH에 얹지 않고 **액션 라우트 셋**(`POST …/tasks/[taskId]/cancel | restore | replace`)으로. 각각 `for update`로 posted_at·cancelled_at·(교체는 visit_on까지) 재검사 후 관련 변경 전체를 한 트랜잭션에 넣는다. PATCH의 `influencerHandle`은 배정(없음→인플)·해제·같은 인플만 받되 **같은 상태 제한(게시 전·취소 아님·방문 전)과 정리 규칙**(해제: 전달됨→사용 확정·증빙 제거 / 배정: 비용은 §4-3 [확인] 흐름)을 서버가 적용. 다른 인플로 바꾸기는 400 "교체는 ··· → 인플루언서 교체로".
- **사유 칩**: 이모지+단어, 값은 영어. 슬랙 관례 ✏️ 원고확인 · 💰 지급 · ❌ 취소(완료 리액션 💰/✅/🤑) — **취소 행동은 ❌**. 기본 선택 없음(누르면 켜짐, 다시 누르면 꺼짐). 거절·무응답 전용 이모지는 슬랙에 없음 → §8.
- **교체**: 게시 전만, 방문협찬은 `visit_on < 오늘`이면 불가(당일·미정은 허용 — 날짜 기준이라 "예약일은 지났는데 실제로 안 왔다"는 방문일을 고친 뒤 교체하거나 취소 후 새 작업; 실제 방문 확인은 저장하지 않는 한계). 비용은 §4-3 흐름(새 인플 단가로 채우고 [확인]). 원고 전달됨→사용 확정. RT 증빙 제거. 사유 선택.
- **로그**: `task_declined`는 사유가 `declined`·`no_response`일 때만, payload `{taskId, campaignId, campaignName, taskType, reason, action:'cancel'|'replace'}`. 인플 미배정 작업은 로그 없음. 되돌리기 정정 이벤트 없음(한계).
- **기존 화면의 게시 확인 경로**(수동·자동 조회·트래킹 연결)는 취소 작업을 건너뛰고 문구로 알린다.

## 7. 다음 단계

1. **구현 계획**(`writing-plans`) — 이 문서 §3~§6 + ADR 0001~0006이 입력. 마이그레이션 055(R16) → 판정 함수·집계 공통화(R17) → 액션 라우트 셋(§6) → 새 페이지 `/campaigns/flow`(표·카드·패널) → 정산 딥링크(R15) → 기존 화면 취소 표시(R20) 순이 자연스럽다.
2. 시안 파일(`~/claude-outputs/20260915_캠페인v2_시안.html`)은 버림 — 확정 내용은 이 문서가 원본.
3. **v2 1.5 — 두 건, 1차 배포 직후**
   - **레퍼런스 고르기 재설계**(보관함 콘텐츠 확인·선택 UX) — 1차의 첫째 불편(Q1 A)이라 미루지 않는다.
   - **예산 기간 모델**(koo 09-18, C안): 클라이언트 월 예산(`monthly_budget` + `budget_overrides`)을 **기간 행**(`시작일 · 종료일 · 금액`, 겹침 금지, 빈 구간 = 미설정)으로 교체. 캠페인은 **시작일이 속한 기간**에 귀속 — 주 단위 캠페인이 달 경계에 걸치는 문제(매달 한 주)를 기간을 월요일 시작으로 맞춰 없앤다. 새 기간 기본값 = 직전 종료 다음 날부터 4주·직전 금액. 현행 월 값은 1일~말일 기간으로 이관. 클라이언트 상세 예산 패널·`spendByMonth`·캠페인 카드 라벨이 바뀜(1~2일). 세부는 그때 grilling 한 라운드.
4. 이번 주 캠페인 일괄 생성, 생성 백그라운드 처리, 성과 기반 레퍼런스 제안은 **후순위**(실사용 뒤).

## 8. 열린 항목

- [ ] 거절·무응답 사유 이모지 — B에서 시안 그대로 구현됨(`CANCEL_REASON_CHIPS` 한 곳, 🙅 거절 · 🔇 무응답 · 📝 기타) — koo 확정 대기, 바꿀 땐 그 한 곳만
- [x] 예산 규칙(09-18): ⓐ 다른 캠페인 구간 = **계획 기준**(현행 유지) ⓑ 추가 비용은 **계획에 포함**(현행 유지), 소진에는 넣지 않음. 월 집계 `spendByMonth`의 '미사용 원고 제외'는 R17에 따라 '취소 제외'로 교체(같은 모집단). 달→**예산 기간** 전환은 v2 1.5(§7-3)
- [ ] 성과 [업데이트] 버튼에 비용 안내 — B에서 금액이 아니라 "게시물 n건 · 게시물당 API 1회"로 구현됨(약 $ 표기 없음) — koo 확인
- [ ] 행동 버튼(게시 확인·원고 열기를 표에서 1클릭) 위치 — **보류**(koo 09-17). 지금은 `···`와 패널
- [ ] 패널 `···` 위치 — B에서 헤더 오른쪽으로 구현됨 — koo "만들어진 거 보고 결정"
- [x] (A 계획 완료 09-18) 구현 시: 마이그레이션 번호 재확인(`ls migrations | tail`, 지금 054까지) · 프로덕션에 "미사용 원고가 붙은 미게시 작업"이 있으면 R17 전환 전 취소로 바꿀지 확인 · 트래킹↔취소 동시 실행에서 DB CHECK 실패가 정상 오류로 처리되는지 테스트 — 055 적용(2026-09-18), 프로덕션 미사용 원고 미게시 작업 = 실데이터 0(점검 시 잡힌 1건은 중단된 테스트 잔재 `tcmp…`였고 정리됨 → 전환할 것 없음), 트래킹↔취소 동시 실행은 최종 리뷰(09-18)에서 DB CHECK 실패가 "정상 오류로 처리"가 아니라 타이밍에 따라
raw 23514로 새기도 하는 진짜 경합임이 드러나 재작업: `updateTask` where 가드(게시 확인만 취소 작업에 막고
메모는 허용, R17·R18) + `linkTrackedPost` 작업 select에 `for update`로 `cancelTask`와 직렬화해 경합 자체를
제거하고, 그래도 다른 경로로 23514가 나면 라우트가 문구로 매핑 — 테스트: `campaignTaskCancel.test.ts`
"7) updateTask R17 가드"·"10) 트래킹↔취소 동시 실행"(두 트랜잭션을 실제로 동시 실행해 for update 없이는
raw PostgresError(23514)로 — 이 음성 갈래는 수정 전 코드로 되돌려 수동 확인 —, 있으면 `TrackingLinkError('cancelled-task')`로 거절됨을 확인). 전체 `npm test`: 이 계획으로 깨진 테스트 0 — 유일한 실패 `campaignStore.test` "정산 배지·삭제 보호"는 09-11 정산 분류 개편 뒤부터의 기존 실패(`settlementStore.test`처럼 before/after에서 기본 분류를 깔면 고쳐짐, 별건)
- [x] A 계획 잔여 minor(최종 리뷰 판정 09-18): replaceInfluencer 같은 핸들 no-op → 머지 전 수정(C3) · 커버리지 보강(taken/gone·명부 없는 핸들·가드 순서·R17 가드·경합) → 테스트 7~10 추가 · savepoint 캐스트·PATCH 가드 중복은 불필요 판정. B로 넘김 중 055 check `not valid`·미사용 인덱스 idx_campaign_task_cancelled는 B에서 해결(4feaa63, 09-19). 남은 건: WeekCalendar 취소 작업의 빈 주 행, `logTaskDeclined` insertAutoLog 재사용
- [ ] 캠페인 페이지 대체 조건·시점, `/generate` 처리 방식 — 안정화 후
- [ ] R25(대상) 부분 구현 — '아직 게시 전인 글이에요' 체크는 화면에만 있고, 그 상태에서 어느 작업인지 고르지 않으면 저장되지 않는다(스키마 변경 없음, `TargetLinkField`의 로컬 `pending` state뿐). 필요해지면 컬럼 하나(예: `target_pending`) 추가
- [ ] 성과 카드에 기준 시각이 없다 — 상세 응답에 최신 스냅샷 시각(`captured_at` 등)이 없어 "언제 기준 숫자인지"가 화면에 안 보인다
- [ ] `perf-refresh` 라우트에 실행 시간 상한이 없다(게시물 수만큼 순차 조회) — 기존 게시 확인 라우트(`check-posted`)도 같은 모양
- [ ] 통화 2종(원·엔)이 섞인 캠페인은 비용 카드 숫자(`formatMoneyBy`)가 " · "로 이어 붙기만 하고 줄바꿈 처리가 없다 — 한 줄이 길어질 수 있다
- [ ] 예산이 0원으로 설정된 달은 막대를 그리지 않는다(`hasBudgetBar = amount !== null && amount > 0`) — "0원"과 "미설정"이 화면에서 구분되지 않는다
- [ ] `REPLACE_AFTER_VISIT_MESSAGE` 문구("방문한 인플루언서가 게시해야 해요")가 교체뿐 아니라 배정·해제(PATCH, `allowReplace` 없이) 실패에도 그대로 나온다(A 이월) — 방문일이 지난 작업에서 인플루언서를 떼기만 해도 같은 문구가 뜬다
- [ ] WeekCalendar에서 취소 작업이 캠페인 기간 밖의 예정일을 가지고 있으면 빈 주 행이 생긴다(A 이월, 기존 화면)
- [ ] 원고 카드 편집을 X 컴포저로 통일 — 지금 `DraftEditModal`(평범한 텍스트 칸) 그대로. `/generate`·기존 캠페인 상세에도 파급되는 별건이라 이번 범위 밖(koo 09-18: 통일이 맞되 크기 보고, §5-2)
- [ ] 직접 쓰기: 이미지는 고르는 순간 올라가고 저장은 [저장하고 붙이기] 한 번인데, 저장하지 않고 떠나면 올라간 파일이 스토리지에 그대로 남는다. 저장 경로에 소유자(어느 원고인지) 정보가 없어, 원고 전체를 훑지 않고는 참조 중인 파일과 고아 파일을 구분할 수 없다
- [ ] 레퍼런스 고르기 재설계 — v2 1.5 예정(§7-3에 이미 기록)
- [ ] 원고 카드에서 **다른 캠페인**의 작업으로 옮기는 기능이 v2엔 없다 — 그 피커는 이번에 없앤 전체화면 오버레이에서만 열렸다. 기존 캠페인 화면에는 남아 있다
- [ ] 로그인이 만료되면 '있는 원고 고르기' 후보 목록과 잠금(busy lock)이 풀리지 않아, 그 탭이 계속 "불러오는 중"이라고 말할 수 있다
- [ ] 후보 목록의 백그라운드 재조회가 실패하면, 화면에 있던 멀쩡한 목록이 실패 화면으로 바뀐다
- [ ] 후보 목록은 200건까지만 읽고 검색도 그 200건 안에서만 돈다 — 미부착 원고가 200건을 넘는 클라이언트는 있는 원고를 검색해도 "없음"으로 보일 수 있다
- [ ] AI로 만들기·직접 쓰기 탭은 서버가 붙이기를 거절해도 화면을 다시 읽지 않아, 같은 오류로 재시도하게 된다
- [ ] 설정 요약이 클라이언트를 아직 불러오는 동안 "시술 없음"이라고 말한다
- [ ] 시안을 다시 만들면 화면의 이전 시안이 새 시안으로 교체된다(더해지지 않는다)
- [ ] 직접 쓰기로 쓴 원고는 시술 스냅샷이 없다
- [ ] 예정일 바꾸기는 컴포저의 저장 안 한 상태만 확인하고, 새 작업 만들기 폼의 저장 안 한 상태는 확인하지 않는다
- [ ] 작업을 취소하면 원고 모드가 닫히고, 되돌리면 원고 모드가 바로 다시 열린다 — 전환이 갑작스럽다

## 9. 결정 이력

| 결정 | 누가·언제 |
|---|---|
| 기획 처음부터 재시작, 옛 결정 전제 금지 | koo 09-14 |
| 8단계 기능은 다 있음, 문제는 흐름 연결 → 한 화면 UX | koo 09-14 |
| 진행이 기본값, 예외(거절·무응답)만 입력 | koo 09-14 (Claude 제안 A 대신) |
| 취소 사유 선택 입력, 이모지+단어 칩, DB 영어 | koo 09-14·15 |
| 예정일 확인 섹션 불필요 → 진행 중/완료/취소 3자리 | koo 09-14, 가안 |
| 새 페이지 병존, 이름 "캠페인 v2" | koo 09-14·15 |
| 정산은 정산 페이지에 남김 | koo 09-14 |
| 원고 생성/고르기는 별도 기획, 시트는 사이드 | koo 09-14 |
| 시안 먼저(정적), 필요하면 동작 프로토타입 | koo 09-15 |
| **Codex 코드 대조 리뷰 16건 → 수용 11·부분 수용 4·질문 3** (원고 복원 정보·취소↔게시 상호 배제·병존 판정 규칙·교체 비용 묻기·대상 취소 표시·편집 경계·로그 payload·미정 배지·정산 대기 분리·추가 비용 범위·용어 대응표·딥링크 0건·가안 표기·ADR 0005) | Claude 검증 09-15 |
| 원고 미사용 ≠ 작업 제외 — 원고는 초안이고 인플이 고치거나 새로 쓰는 일이 흔함 → 집계 제외는 취소만 | koo 09-15 |
| 방문 후 인플 교체 불가 — 방문한 사람이 게시해야 함 | koo 09-15 |
| 취소 작업 명시적 삭제 허용 | koo 09-15 |
| **시안 4라운드(D→E→F)**: 세로 카드형(D·E) 거절 → "행 하나 = 한 줄" C 기반 F 채택 · 자리·유형 열 복원 · 대상은 표에서 빼고 원고는 첫 줄 발췌 | koo 09-17 |
| 다음 행동 열 → **단계** 열, 큰 단계 6개(준비·전달·게시·정산·완료·취소 — 진행→전달·완료 추가는 koo 제안) | koo 09-17 |
| 필터 띠 → 드롭다운 하나(중복 선택) + 유형 묶음 + 검색 한 칸 / 날짜 색 규칙·미정 표기·볼드 없음·지연 행 배경 없음 / 정렬 만든 순 + 헤더 클릭 / 인플 링크는 넣었다 취소 | koo 09-17 |
| 요약 카드: 작업·성과·비용 3장, 유형별 카드 삭제, 예산 3개념(월·계획·소진)을 막대 하나로, 문구 6원인 진단 후 다시 씀 | koo 09-17~18 |
| **①② 생성 워크플로 grilling 3라운드**: 뼈대(유형별 개수) 먼저 → 채우기 / 입구 둘 / 행 클릭 = 편집 패널(Codex 10번 반대를 뒤집음) / 칸 순서 / 비용 [확인] 3시나리오 / 대상 링크 통일 + 게시 전 체크 / 패널·시트는 한 표면 / 일괄 생성 후순위 | koo 09-18 |
| 패널 폭 560 · ···는 헤더 · 새 작업은 [만들기]로 생성 | koo 09-18 |
| 예산: 다른 캠페인 = 계획 기준 · 추가 비용은 계획에 포함 · 소진 = 게시 완료 작업 비용만 · 월 집계도 취소 제외로 / **예산 기간 모델(C안) 채택, v2 1차는 현행 월 규칙·1.5에서 전환** | koo 09-18 |
| 직접 쓰기 = X 컴포저(아바타·카운터·스레드·이미지, 이모지·투표 제외) · [저장하고 붙이기] · 카드 편집도 같은 컴포저(크기 보고) / 레퍼런스 고르기 재설계는 v2 1.5 | koo 09-18 |
| **원고 모드 grilling 2라운드**: 불편 = 레퍼런스 고르기 + 화면 왕복 / 매번 바꾸는 건 레퍼런스·방향성만(설정 접힘) / 성과 자동 제안 제외 / 시안 3 / 시안 카드는 고르기만, 손보기는 붙인 뒤 DraftCard(패널 안) / 생성 중 머무름 / 고르기 두 묶음 — 시안에서 1~6단계 흐름 확인 | koo 09-18 |
| **시안 수 기본값 1로 확정**: 09-18의 '기본 3'을 뒤집는다. 시안 수를 기억하면 한 번 5로 둔 사람이 이후 모든 생성에서 조용히 N배를 내므로 기억을 없앴고(`/generate`의 기존 안전장치와 같은 이유), 시작값도 `/generate`와 같은 1로 맞췄다. 여러 개가 필요하면 `▸ 설정`에서 올린다 | koo 09-19 |
| **Codex 3차 2건 → 전부 수용**: 배정·해제에도 같은 상태 제한 + 해제는 흔적 정리·배정은 비용 질문(해제→재배정 우회 차단) · 되돌리기는 외부 트랜잭션 하나 안 ①복원 ②세이브포인트 재부착 ③단일 커밋 | Claude 검증 09-15 |
| **Codex 재검토 6건 → 전부 수용**: 트래킹은 가드가 아니라 연결 거절(롤백) · 되돌리기 재부착은 세이브포인트 · 취소/되돌리기/교체는 서버 액션 라우트+한 트랜잭션, 배정·해제·재선택과 교체 구분 · 방문 경계 = `visit_on < 오늘`(당일 허용) · ⚠는 전달할 원고가 남았을 때만 · ADR 문장 충돌 3곳 정리 | Claude 검증 09-15 |
| 원고 모드(C 계획) 구현 완료, 배포 안내 작성 — 원고 카드 편집의 컴포저 통일은 1차에서 제외해 §8로 이관(koo 확인 대기), 다른 캠페인으로 원고 옮기기는 v2에서 빠짐(기존 화면엔 있음) | Claude 2026-09-19 |
| **화면 완성 뒤 koo 결정 3건**: ① 원고 붙는 유형(투고·인용RT·방문협찬)은 원고 없으면 준비(전달 → 원고 붙일 때 역행 방지, RT는 예외) ② 패널에 인플루언서 [해제] 추가(PATCH, 서버 가드 재사용) ③ 게시 '내림' 표시·되돌리기가 v2에도 필요(표·하단 줄·패널) | koo 09-19 |
