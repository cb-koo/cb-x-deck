# cb-x-deck 합류 안내

X(트위터) 콘텐츠 기획·제작부터 인플루언서 배정, 게시물 추적, 정산까지 한 곳에서 하는 사내 도구입니다.
사용자는 비개발 콘텐츠 기획 담당자 소수(현재 4명)이고, 운영 중입니다.

이 문서는 **처음 이틀 안에 시간을 잃지 않기 위한 것**입니다. 기능 설명이 아니라, 모르면 반드시 헤매는
것들만 모았습니다. 상세 규칙은 `AGENTS.md`, 화면·명령 목록은 `README.md`에 있습니다.

---

## 1. 시작하기

```bash
git clone <repo> && cd cb-x-deck
npm install
```

**설정 파일 두 개가 필요합니다. 둘 다 git에 없으니 koo에게 받으세요.**

| 파일 | 무엇 | 쓰이는 곳 |
|---|---|---|
| `.env` | 운영 DB + 외부 API 키 전부 | 로컬 실행, 배포, 운영 데이터 조회 |
| `.env.staging` | 연습용 DB | `npm test` |

`.env.staging`은 `scripts/make-staging-env.sh`로 다시 만들 수 있습니다. 연습용 DB 비밀번호만 있으면
나머지 값은 스크립트가 Supabase CLI로 채웁니다. 비밀번호는 조회가 안 되고 Supabase 대시보드에서
재설정만 됩니다. **재설정 직후 1~2분은 인증이 거부됩니다. 비밀번호가 틀린 게 아니니 잠시 뒤 다시 하세요.**

---

## 2. 반드시 알고 시작할 것

**① `npm run dev`를 쓰지 마세요.** 이 저장소의 Next 빌드에서 dev 모드가 하이드레이션을 조용히
실패시킵니다. 콘솔 에러도 없고 네트워크도 200인데 모든 버튼이 죽은 HTML이 됩니다. 화면을 봐야 하면
이렇게 하세요.

```bash
npm run build && npx next start -p 3001   # 그리고 http://127.0.0.1:3001
```

주소는 `localhost` 말고 **`127.0.0.1`**을 쓰세요. 이 환경에서 크롬의 localhost 접속이 정체불명의
로컬 프록시에 잡혀 무한 로딩됩니다. 더 편한 방법은 `vercel`(--prod 없이)로 프리뷰 배포를 만드는 것인데,
로그인 리다이렉트 허용 목록 설정이 선결이라 처음 쓸 때 koo와 확인하세요.

**② 로컬 실행은 운영 DB에 붙습니다.** 별도 개발용 DB가 없습니다. `npm run dev`나 `next start`로
띄운 화면에서 무언가를 저장하면 실제 데이터가 바뀝니다. 실험은 연습용에서 하세요.

**③ 테스트는 연습용 DB에서만 돕니다.** `npm test`가 `.env.staging`을 씁니다. 실수로 운영을 가리키면
시작 전에 막힙니다. 전체 실행에 **약 17분** 걸립니다(연습용이 무료 요금제라 느립니다). 운영에서
돌려야 할 이유가 생기면 `npm run test:prod`가 있지만 평소엔 쓰지 마세요.

DB를 쓰는 테스트는 33개뿐이고 나머지 99개는 접속 정보 없이도 돕니다. 빠르게 확인하려면 파일을
지정해서 돌리세요.

```bash
node --import tsx --test src/lib/<파일>.test.ts
```

**④ git push로 배포되지 않습니다.** 배포는 명령줄 전용입니다.

```bash
vercel --prod --yes   # ~/cb-x-deck 에서
```

그래서 main에 머지해도 사용자에게는 아직 안 나간 상태입니다. 반대로 말하면 **배포는 항상 의도해서만**
일어납니다. 배포 전에 `.vercel/project.json`이 `cb-x-deck`을 가리키는지 확인하세요. 워크트리를 새로
만들면 폴더 이름으로 엉뚱한 Vercel 프로젝트에 연결된 적이 있습니다.

**⑤ 린트 경고 24개는 기준선입니다.** 고치려 하지 마세요. 표 컨테이너 예외 등 의도된 것들입니다.
숫자가 24에서 늘지만 않으면 됩니다.

**⑥ 마이그레이션 번호는 먼저 잡으세요.** `migrations/NNN_이름.sql`의 앞 세 자리가 적용 순서입니다.
다음 번호는 `ls migrations | tail -1`로 확인하고, 브랜치를 시작할 때 **빈 파일이라도 먼저 커밋해
번호를 선점**한 뒤 알려주세요. 두 사람이 같은 번호를 쓰면 순서가 깨집니다. 적용은 운영과 연습용 양쪽에
합니다.

```bash
npm run migrate            # 운영
npm run migrate:staging    # 연습용 (빠뜨리면 테스트가 엉뚱한 이유로 실패합니다)
```

이미 올린 마이그레이션 파일은 번호도 내용도 고치지 않습니다. 다른 사람 DB에 이미 적용됐을 수 있습니다.

---

## 3. 이 저장소의 규칙

**사용자 대면 문구는 전부 한국어이고, 기획 담당자 기준으로 씁니다.** 내부 개념어를 버튼이나 라벨에
그대로 노출하지 않습니다. 자세한 원칙 6개가 `AGENTS.md` 맨 위에 있습니다. UI를 만들기 전에 한 번
훑어보세요.

**사용자에게 보이는 변화를 main에 머지할 때는 `src/content/updates.ts` 맨 위에 항목을 추가합니다.**
배포되면 `/updates` 화면에 자동으로 나타납니다. 화면 변화가 없는 내부 작업도 "달라지는 점 없음"으로
적습니다. 형식은 `src/lib/updates.test.ts`가 검사합니다.

**코드 주석은 "왜"를 적습니다.** 이 저장소의 주석 상당수가 과거에 실제로 겪은 사고의 기록입니다.
같은 실수를 막는 장치이니 지우지 말고, 새로 만든 판단도 같은 식으로 남겨 주세요.

**디자인 시스템을 새로 도입하지 않습니다.** 트윗을 X에서 보이는 그대로 판단하는 것이 이 도구의 존재
이유라, X 화면을 그대로 흉내 내는 부분은 건드리지 않습니다.

---

## 4. 맡으실 일: 비용·성과 데이터 전송 API

### 본보기가 이미 있습니다

정산 프로덕트와 연동하는 API가 같은 모양으로 이미 돌고 있습니다. **새로 설계하지 말고 이걸 따라
가세요.**

| 문서 | 내용 |
|---|---|
| `docs/api/settlement-external-api.md` | 상대 개발자에게 준 명세 (필드표·에러·예시) |
| `docs/superpowers/specs/2026-08-28-payment-api-design.md` | 왜 그렇게 설계했는지 (결정 이력) |
| `docs/api/settlement-handoff/` | 상대와 주고받은 기록 24건 (번호순) |
| `src/app/api/external/settlement/requests/` | 실제 라우트 |

구조는 이렇습니다.

- **인증**: 소비자마다 공유 시크릿 하나. `bearerAuthorized(req, 'ENV_이름')`. 환경변수가 비어 있으면
  전부 거부합니다(열린 API가 되지 않습니다).
- **페이징**: `(updated_at, id)` 커서. 마지막 30초 구간은 내보내지 않습니다. 커밋 직전 행을 커서가
  지나쳐 영원히 안 보이는 것을 막기 위한 안전 지연입니다.
- **호출 기록**: `recordExternalCallSafe`로 모든 호출을 남깁니다. 정산 화면의 연동 기록 탭에서 봅니다.
  "그쪽이 진짜 가져갔나"를 확인할 유일한 근거라 반드시 넣으세요.
- **값의 성격**: 요청 시점 값을 고정할지(스냅샷), 매번 최신을 보낼지(라이브)를 필드마다 정합니다.
  기본은 스냅샷입니다. 라이브로 두면 값이 바뀌어도 `updated_at`이 그대로라 상대 폴링이 다시 가져가지
  않습니다. 이 함정을 꼭 기억하세요.

### 데이터가 있는 곳

**비용**

- `campaign_task.cost` — 작업 단가(jsonb: 금액·통화)
- `payment_request` — 실제 정산 요청. `amount_krw`(단가), `gross_krw`(수수료 포함 실제 송금액을
  원화 환산한 값), `payout`(통화별 금액), 상대가 보낸 실지급액까지
- `client.monthly_budget` / `budget_overrides` — 클라이언트 월 예산

집계에 쓸 "실제 나간 돈"은 `amount_krw`가 아니라 `gross_krw`입니다. 둘이 다릅니다.

**성과**

- `tracked_post` + `post_metric_snapshot` — 게시물별 조회·좋아요·리포스트·답글·북마크·인용.
  스냅샷은 시간마다 한 줄씩 쌓입니다(덮어쓰지 않음)
- `tracking_link` + `link_click_snapshot` — 트래킹 링크 클릭 수
- `landing_event` — 랜딩페이지 도착·탭 이벤트
- `report_snapshot` — 외부 클리닉 마케팅 리포트 수집분(일·주·월)

### 지켜야 할 것

**테스트 데이터가 상대에게 새지 않게 하세요.** 2026-09-09에 실제로 사고가 났습니다. 테스트가 운영
DB에 만든 정산 요청이 상대 폴링에 잡혀 유령 건 10건이 처리됐고, 테스트가 끝나며 우리 쪽에서만
사라졌습니다. 지금은 `src/lib/settlementTestFixture.ts`가 테스트 접두어를 서버에서 걸러냅니다.
**내보내는 API를 새로 만들면 같은 필터를 넣으세요.**

**운영 데이터를 손으로 지우지 마세요.** 상대가 보낸 결과(`external_*` 필드)를 수동으로 초기화했다가
지급 완료 기록이 사라진 적이 있습니다. 다시 받아야 하면 `updated_at`만 갱신해서 상대가 다시 보내도록
합니다.

**명세를 바꾸면 상대에게 알립니다.** `docs/api/settlement-handoff/`처럼 번호를 붙여 문서로 남기고
전달하는 방식입니다. 필드를 추가하는 것은 상대 파서를 깨지 않지만, 쓰려면 매핑을 넣어야 하니 알려야
합니다.

---

## 5. 막히면

- 화면·동작 관련 판단은 koo에게 물어보세요. 사용자가 곧 koo입니다.
- 과거에 왜 그렇게 했는지는 대부분 `docs/superpowers/specs/`의 날짜별 설계 문서에 남아 있습니다.
- 커밋 메시지도 "무엇을 왜"를 적어두는 편이라 `git log --grep`이 잘 듣습니다.
