# cb-x-deck UI/UX 개선 원칙 체크리스트 (확장판)

> **이 문서의 위치**
> - 사내 슬라이드("디자이너들은 뭘 볼까 / CRAP")를 **기준선**으로 삼고, 그 안에 없는 **추가 UI/UX 축**을 발굴·정리한 실무 체크리스트.
> - 작업 순서 **2 → 1** 중 **2단계(보편 원칙 지도)** 산출물. 다음 단계(1)에서 이 체크리스트로 우리 서비스 실제 화면을 조준한다.
> - 사용법: 화면을 만들거나 고칠 때 각 축의 **체크항목**을 훑고, 위반이 있으면 구현 전에 고친다. (AGENTS.md UX 원칙과 함께 사용)
> - 작성 2026-07-24 · **확장 2026-07-29(M~P: 생성 기능 축 4개 추가)** · Exa 리서치 기반
> - 우리 서비스 = **비개발 콘텐츠 기획자용 X(트위터) 리서치·브리핑·데이터 대시보드 + 콘텐츠 생성**
> - 축이 16개다. **화면 성격에 맞는 축만 고른다** — "축 적용 범위" 표 참조.

---

## 슬라이드가 이미 커버하는 것 (중복 정리 안 함)
- **1단계 Less is More** — 핵심만 남기고 다 제거
- **2단계 CRAP** — Contrast(대비) · Repetition(반복) · Alignment(정렬) · Proximity(근접)
- **조형 요소/원리** — 점·선·면·색·명도·질감 / 균형·대비·강조·위계·비례·리듬·반복·통일·여백

→ 아래는 **이 위에 얹을 확장 축 16개** (A~L 원본 12개 + M~P 생성 기능 4개).

---

## A. 게슈탈트 원리 (CRAP의 뿌리 + 확장)
CRAP의 Proximity/Repetition은 게슈탈트의 일부일 뿐. 나머지도 도구로 챙긴다.

| 원리 | 한 줄 정의 | 우리 서비스 체크항목 |
|---|---|---|
| 유사성(Similarity) | 비슷하게 생기면 같은 기능으로 읽힌다 | 같은 역할의 카드·버튼·지표 타일은 **모양·색·크기 통일**. 다르게 보이면 다르게 동작한다는 신호가 되니 주의 |
| 공통 영역(Common Region) | 같은 배경/테두리 안이면 한 그룹 | 지표 그룹, 필터 묶음을 **카드/배경면**으로 묶어 소속을 명확히 |
| 연속성(Continuity) | 눈은 부드러운 선/정렬을 따라간다 | 다단계 흐름(로그인→분석→결과)·리스트를 **정렬 축**으로 이어 시선 끊기지 않게 |
| 폐쇄성(Closure) | 일부만 보여도 뇌가 완성한다 | 캐러셀/스크롤 목록은 **다음 항목을 살짝 걸쳐** "더 있다"를 암시 |
| 전경-배경(Figure-Ground) | 주인공과 배경을 분리 | 모달은 **어두운 오버레이**, 클릭 가능한 요소는 배경 대비로 "눌러진다" 신호 |
| 초점(Focal Point) | 대비가 시선을 한 점에 모은다 | 화면당 **주인공 1개**(핵심 숫자/주요 CTA)만 강조 |

출처: reallygooddesigns.com/gestalt-principles-ui-ux · figma.com/resource-library/gestalt-principles · blurtest.com/blog/gestalt-principles-in-ui-design-a-practical-guide

---

## B. Nielsen 사용성 휴리스틱 10 (진단 프레임)
화면을 "예쁜가"가 아니라 "쓸 수 있는가"로 점검하는 표준 10항목.

1. **시스템 상태 가시성** — 지금 뭐가 되고 있는지 항상 보여줌(로딩·진행·완료)
2. **현실 세계와 일치** — 사용자 언어로. 내부 개념어 금지 *(AGENTS.md 원칙1과 동일)*
3. **사용자 통제·자유** — 실수 취소(Undo)·되돌아갈 출구
4. **일관성·표준** — 같은 뜻은 같은 말·같은 위치. 업계 관습 따르기
5. **오류 예방** — 좋은 에러 메시지보다 **애초에 오류가 안 나게** 설계
6. **회상보다 인식** — 외우게 하지 말고 **보이게**(옵션·최근 항목 노출)
7. **유연성·효율** — 초보엔 안 보이되 숙련자엔 단축키/고급 경로 제공
8. **심미적·미니멀** — 관련 없는 정보는 뺀다 *(Less is More와 연결)*
9. **오류 인식·복구 지원** — 에러코드 대신 **원인+해결책**을 평문으로
10. **도움말·문서** — 이상적으론 불필요하되, 필요하면 맥락 안에서 제공

체크: 새 화면을 이 10개로 훑고 위반 지점에 심각도(0~4) 매겨 우선순위화(heuristic evaluation).
출처: nngroup.com · heurilens.com/blog/nielsens-heuristics · blog.logrocket.com/ux-design/conduct-heuristic-evaluation-tutorial

---

## C. UX 법칙 — 인지 부하 (Laws of UX)
"핵심만 남기기"를 왜/어떻게 하는지의 심리학적 근거.

| 법칙 | 내용 | 우리 서비스 적용 |
|---|---|---|
| **Hick's Law** | 선택지 많을수록 결정 시간 ↑ | 필터·버튼·옵션 수 줄이기. 못 줄이면 **점진적 공개**로 필요할 때만 노출. 추천값을 강조해 결정 유도 *(AGENTS.md 원칙3 '추천' 연결)* |
| **Miller's Law** | 작업기억은 7±2개 | 지표·링크를 **덩어리(chunk)**로 묶기. 긴 리스트는 4~5개 헤딩 아래로 그룹화 |
| **Fitts's Law** | 타깃은 크고 가까울수록 빨리 누른다 | 주요 CTA·터치 타깃 **충분히 크게**(최소 44×44px), 자주 쓰는 건 손닿는 위치 |
| **Jakob's Law** | 사용자는 다른 사이트에 익숙 → 관습대로 | 검색은 우상단, 로고는 홈, 장바구니 아이콘 등 **표준 패턴** 따르기(새로 학습시키지 말 것) |

출처: lawsofux.com · uxdesigninstitute.com/blog/laws-of-ux · myuxacademy.com/blog/ux-design-laws-principles

---

## D. 시각적 위계 (Contrast의 실전 확장)
"무엇을 먼저 보게 할까"를 크기·대비·여백·위치로 설계.

- **3단계 크기 규칙** — 헤더/서브헤더/본문 정도로 크기는 3개까지만. 위계는 개별 크기가 아니라 **단계 간 대비**에서 나온다(48px 얇은 글씨 < 24px 굵은 글씨).
- **위계는 색만이 아니다** — 크기·굵기·자간·행간·모션·밀도(주변 여백)로도 만든다. 여백 섬에 놓인 요소는 실제 크기보다 중요해 보인다.
- **스캔 패턴** — 글 많은 화면은 **F 패턴**, 요소 적은 화면은 **Z 패턴**. 핵심을 눈이 실제 머무는 곳(상단·좌측)에 배치. 사용자는 시청 시간의 ~57%를 첫 화면(above the fold)에 쓴다.
- **마이크로 위계** — 라벨과 값, 버튼 라벨과 아이콘의 굵기/간격 관계까지 일관되게. 여기가 어긋나면 "덜 다듬어진" 느낌을 사용자가 감정적으로 먼저 느낀다.

출처: nngroup.com/articles/visual-hierarchy-ux-definition · ixdf.org/literature/topics/visual-hierarchy · tubikstudio.com/blog/visual-hierarchy-effective-ui-content-organization

---

## E. 대시보드 / 데이터 시각화 ★ (우리 서비스 핵심 축)
데이터를 "덤프"가 아니라 "결정 도구"로. Tufte·Stephen Few 기반.

- **Data-Ink 비율 최대화** — 데이터가 아닌 잉크(테두리·격자·그림자·3D·장식)는 최소화. 격자선은 옅게 또는 제거.
- **차트는 질문에 맞춘다**
  - 시간 추이 → **선(line)** (X축=시간, 계열 5개 이하)
  - 카테고리 비교 → **막대(bar)** (값 순 정렬, 7개 이하, 막대차트 Y축은 0부터)
  - 부분/전체 → 5+개면 누적막대, 2~4개만 파이/도넛
  - 단일 지표 → **지표 카드(Metric Card)**
  - 상세 열거가 목적이면 → **표**(타일 벽 금지)
- **KPI 카드** — 화면 최상단 큰 숫자(48px+), **각 지표엔 반드시 비교 맥락**(전기간 대비 증감). 3~6개만. 15개 늘어놓으면 아무것도 전달 못 함. *(우리 /usage 스토리텔링과 직결)*
- **한 화면 = 몇 초 읽기** — 히어로 차트 1개 + 보조 지표 몇 개 > 경쟁하는 차트 5개. "모두 강조 = 아무것도 강조 안 됨."
- **정확도 순 인코딩** — 사람은 위치 > 길이 > 각도 > 면적 > 색 순으로 정확히 읽음. 중요한 비교는 가장 정확한 채널(위치/길이)로. 색은 보조 신호로만.
- **위젯 5~7개/뷰**, 각 위젯 내부 구조 통일(제목=좌상단, 컨트롤=우상단). 기본 상태가 한눈에 답을 주고, 인터랙션(필터·드릴다운)은 그 위에 얹을 때만 가치 있음.

출처: desisle.com/resources/saas-dashboard-design-guidelines · perceptualedge.com(Stephen Few) · valiotti.com/dashboard-design-rules · setproduct.com/blog/dashboard-ui-design

---

## F. Norman 6원칙 (직관성의 기반)
"매뉴얼 없이 이해되는가" = discoverability.

| 원칙 | 뜻 | 체크 |
|---|---|---|
| 어포던스(Affordance) | 생김새가 가능한 행동을 알림 | 버튼은 눌러질 것처럼, 입력칸은 채워질 것처럼 보이게 |
| 시그니파이어(Signifier) | 어디를/어떻게를 알려주는 단서 | 라벨·아이콘·힌트로 "여기 누르세요"를 명시 |
| 매핑(Mapping) | 컨트롤과 결과의 자연스러운 대응 | 좌=이전, 우=다음 등 공간적 직관 맞추기 |
| 피드백(Feedback) | 모든 행동에 즉각·명확한 반응 | 클릭·저장·API 콜에 상태 변화를 즉시 보여줌 |
| 제약(Constraint) | 잘못된 행동을 막음 | 불가능한 옵션은 비활성/숨김으로 실수 원천 차단 |
| 개념 모델 | 머릿속 작동 그림 | 사용자가 예측 가능한 일관된 동작. 거짓 어포던스(눌러도 안 되는 것)는 신뢰를 깬다 |

출처: Norman "The Design of Everyday Things" · uxmag.com/articles/understanding-don-normans-principles-of-interaction · textbookofusability.com

---

## G. UI 상태 설계 (AI가 자주 빠뜨리는 것)
모든 데이터/빈 뷰는 **4가지 상태**를 명시적으로 설계. 빈 화면+무설명 금지.

- **로딩** — 200ms 후 **스켈레톤**(최종 레이아웃과 동일). 중앙 스피너+빈 화면은 지양. 스켈레톤은 체감 로딩을 ~30% 줄이고 레이아웃 시프트 방지.
- **빈 상태(Empty)** — ①왜 비었는지 ②다음 할 행동(단일 CTA) ③채워지면 뭐가 보일지. "데이터 없음"은 기회 낭비. 필터 결과 0건과 신규 사용자 0건을 구분.
- **에러(Error)** — 평문으로 ①무엇이 잘못됐나 ②어떻게 복구(재시도/뒤로/문의) ③되돌아갈 길. 스택트레이스·에러코드 노출 금지, 사용자 탓 금지. (지원용 에러 ID는 병기 가능)
- **성공/부분/오프라인** — 성공은 색만이 아닌 시각 피드백. 여러 소스면 부분 로드 처리.
- (고급) **낙관적 UI** — 좋아요·북마크 등은 즉시 반영 후 실패 시 롤백.

체크: 새 뷰는 **로딩·빈·에러·정상** 4개를 JSX 쓰기 전에 먼저 설계.
출처: blog.vibecoder.me/empty-states-loading-states-error-states · agenticdevelopercookbook.com/guidelines/implementing/ui/state-design · skills.smoothui.dev/docs/state-design

---

## H. 마이크로카피 / UX 라이팅
작은 글자가 전환·이해를 좌우. *(AGENTS.md 원칙1·2·3의 실행 지침)*

- **버튼 = 동사 + 결과** — "제출/확인" ✕ → "리포트 다운로드", "계정 만들기" ○. 1~4단어. "무엇을 하면 무엇을 얻는지".
- **3C** — Clarity(명료) > Concision(간결) > Character(개성) 순 우선. 명료가 최우선.
- **플레인 랭귀지** — 전문용어(min_faves, probe, density…)는 옆에 뜻을 붙이거나 사용자 언어로. *(AGENTS.md 원칙5)*
- **헬퍼 텍스트** — 입력/버튼 옆에 "무엇→무엇" 한 줄(progressive disclosure, 필요한 자리에만).
- **에러 카피** — 무엇이 잘못됐고 어떻게 해결하는지.
- **결과 서술** — 숫자만 던지지 말고 해석+다음 행동. "좋아요 53~346" ✕ → "반응이 드문 편(53~346) → min_faves 100 추천 [적용]" ○ *(AGENTS.md 원칙3)*

출처: nngroup.com/articles/3-cs-microcopy · nngroup.com/articles/3-is-of-microcopy · productwithmustache.com/ux-writing-microcopy

---

## I. 접근성 (WCAG 실무 최소선)
빼기 어려운 기본기. 색약·저시력·키보드 사용자까지.

- **대비** — 본문 텍스트 **4.5:1** 이상(큰 텍스트 3:1), UI 경계·아이콘 **3:1** 이상.
- **색만으로 의미 전달 금지** — 빨강=에러에 **아이콘(✕/⚠)·텍스트·위치**를 함께. 필수 항목은 색이 아니라 별표+"필수".
- **키보드** — 모든 기능 키보드로 조작 가능, 키보드 트랩 없음, **포커스 표시 항상 보이게**(2px 아웃라인).
- **터치 타깃** — 최소 44×44px (Fitts's Law와도 연결).
- **가독성** — 본문 14px+(16px 권장), 행간 1.5+, **200% 줌에도 가로 스크롤 없이**.
- **모션** — `prefers-reduced-motion` 존중.

출처: w3.org/TR/WCAG21 · deficiencyview.com/blog/wcag-accessibility-beyond-color-contrast-guide · sujeet.pro/articles/wcag-2-2-practical-guide · ada.gov/resources/web-guidance

---

## J. 디자인 시스템 기반 (Repetition을 시스템으로)
일관성을 눈대중이 아니라 **토큰**으로 강제.

- **8pt 그리드** — 간격은 8의 배수(4·8·12·16·24·32·48·64…). 1×/1.5×/2× 디스플레이에 깔끔히 스냅. Material·Apple·Primer·Atlassian 공통.
- **타입 스케일** — 모듈러 스케일(비율 1.125~1.25). 임의 크기(14/17/22/28) 대신 한 비율에서 파생. Stripe식 "조용한 위계".
- **개수 제한** — 타입 6~8개, 간격 토큰 6~8개. 더 많으면 위계가 흐려지고, 적으면 즉흥이 된다.
- **간격의 의미** — 관련 콘텐츠 4~16px, 액션과 콘텐츠 사이 16~32px, 섹션 구분은 더 크게. "이 요소들은 친구인가?"로 자문.
- **토큰화** — 색·간격·타입을 CSS 변수/토큰으로 통일 → 반복·일관성이 자동으로 유지됨.

출처: atlassian.design/foundations/spacing · paste.twilio.design/foundations/spacing-and-layout · alltools.dev/tools/design/spacing-scale-calculator

---

## K. 비개발자 / 저디지털 리터러시 배려 ★ (우리 사용자 정의)
우리 사용자 = 비개발 기획자. 속도보다 **확신**을 준다.

- **확신·안심이 핵심 지표** — 이들은 "빠름"보다 "내가 제대로 했다"는 확신을 원함. 실수로 뭘 망칠까 두려워함.
- **전문용어 제거·인간적 언어** — Submit/Proceed/Authenticate/Verify 같은 딱딱한 말 피하기.
- **선택지 최소화** — Skip/Save/Continue/Maybe Later 동시 노출은 초보에게 "어느 게 정답?" 불안을 줌. 가이드가 되어라(게이트키퍼 X).
- **점진적 공개** — 한 번에 다 설명 X. 필요해질 때 고급 기능 노출. 첫 화면은 핵심 1~2개 행동만.
- **맥락 안내** — 막혔을 때 화면을 떠나지 않고 그 자리에서 팁·마이크로카피 제공.
- **기준 질문** — "쓰기 쉬운가?"가 아니라 **"도움 없이도 이해되는가?"**

출처: ryviu.com/blog/how-to-design-an-onboarding-experience-for-non-technical-users · uxhorizon.com/designing-for-low-digital-literacy · uxdesigninstitute.com/blog/ux-onboarding-best-practices-guide

---

## L. 즉시 캡처 · 인라인 생성 · 입력 마이크로인터랙션 ★ (행동 지점 흐름)
우리 서비스엔 "덱에서 트윗을 보다가 → 저장 → 한 줄 메모"처럼 **판단이 선 순간 바로 흔적을 남기는** 흐름이 많다. 이 순간의 마찰을 설계로 없앤다. (첫 적용: 덱 ☆ 저장 시점 인라인 메모 캡처)

### L-1. 캡처는 마찰 없이, 정리는 나중에 (Capture ≠ Organize)
- **캡처와 정리는 별개 활동.** 저장·메모를 남기는 순간엔 "외재화"만 시키고 태그·분류·정리는 나중(보관함)으로 미룬다. **캡처 시점에 분류를 강제하면 마찰이 급증**한다.
- **의도→기록 사이 결정 지점을 0에 가깝게.** "저장할까? → 어디에? → 무슨 형식? → 제목은?" 각 결정이 통찰을 흘린다. 필수 필드 0개, 커서만 준비.
- **"사람 속도로 캡처, 기계 속도로 정리."** 캡처 순간엔 최대 충실도·최소 마찰, 나머지는 잊지 않는 시스템(보관함)에 위임. 임계는 대략 **5초/1제스처** — 그 이하면 "할까 말까" 계산 자체가 사라진다.
- 우리 체크: 캡처 입구(☆)는 **1클릭 즉시 저장 유지**, 어떤 결정도 얹지 않는다. 메모는 **선택적**(강제·필수 아님)으로 저장 직후 그 자리에서. 정리(수정·태그·팀 코멘트)는 보관함으로 미룬다.

### L-2. 현재 맥락 안에서 생성 (인지 거리 최소화, pogosticking 금지)
- 새 데이터는 **사용자가 지금 있는 화면에서 바로** 만들 수 있게. 별도 페이지로 보내 왕복(pogosticking)시키지 않는다.
- 작고 잦은 생성·수정 = **인라인(edit/expand-in-place)**. 모달의 open→save→close 세금, 페이지의 navigate-away→come-back 세금을 피한다.
- 모달·페이지 이동은 **방해가 목적일 때만**(파괴적 확인 등). 카드가 그 자리에서 펼쳐지는 inline expansion이 오버레이·내비게이션 없이 방해가 가장 적다.
- 우리 체크: 저장 메모를 보관함으로 보내 왕복시키지 않고 **덱 카드 안에서 인라인 펼침**으로 잡는다.

### L-3. 입력/컴포저 마이크로인터랙션 (기본기 체크리스트)
- **IME 조합 중 Enter 무시**(`isComposing`) — 한국어/일본어 오전송 방지. *(우리 코드 이미 준수)*
- **단일행 = Enter 제출**, 멀티행(textarea) = Cmd/Ctrl+Enter 제출·Enter는 줄바꿈(하나로 정하고 일관되게).
- **autofocus** — 펼침과 동시에 커서 준비(데스크톱). time-to-첫키를 0으로. (모바일은 키보드 팝업 주의)
- **Escape로 닫기**, 입력이 있으면 폐기 확인 또는 보존.
- **입력 보존** — 실패·중단·포커스 이탈에도 초기화 금지. 성공 제출 후에만 비운다.
- **placeholder ≠ 라벨** — 포커스 시 사라지므로 뜻은 힌트/라벨로. 힌트는 입력 위, 에러는 입력 아래(읽기 순서).
- **빈/공백 제출 거부**(trim), 제출 중엔 라벨을 로딩 상태로 바꾸되 포커스는 유지.
- **낙관적 반영 + 성공 피드백** — 저장은 즉시 시각 반영, 성공은 색만이 아닌 명시 신호(F-피드백·G-낙관적 UI와 연결).

출처: howtothink.ai/learn/capture-must-be-frictionless · get.mem.ai/guides/capture-ideas-without-losing-thought(5초 임계) · Tiago Forte BASB(capture≠organize) · uxmatters.com/mt/archives/2024/12/cognitive-distance · smashingmagazine.com/2026/03/modal-separate-page-ux-decision-tree · saasui.design/blog/saas-modal-dialog-ux-patterns · frontendpatterns.dev/prompt-input(IME·Enter) · skills.smoothui.dev/docs/forms · github.com/GoogleChrome/modern-web-guidance(forms)

---

## M. AI 생성물의 근거 표시 ★ (생성 기능 전용)
LLM이 만든 산출물은 "그냥 믿으라"고 요구하면 안 된다. **무엇을 넣었더니 이게 나왔는지**를 보여준다.
(추가 2026-07-29 · 콘텐츠 생성 기능 설계 중 발굴 · Exa 리서치 기반)

- **답 / 근거를 층으로 분리** — 한 덩어리로 합치면 사용자가 판단할 재료가 없다. 산출물 옆에 "무엇에 근거했는가"를 별도 표면으로.
- **근거는 평문 한 줄로 먼저** — "내부 레퍼런스 3건 기반"처럼. 그 다음에 목록.
- **출처는 클릭 가능해야 한다** — 장식용 인용 표시 금지. 눌러서 원본을 확인할 수 있어야 근거다. (안 눌러도 "감사 가능하다"는 신호만으로 신뢰가 올라간다)
- **반영 안 된 것도 정직하게 표시** — 일부만 반영됐는데 전체가 반영된 것처럼 보이면 근거 표시가 오히려 해가 된다. 껐으면 껐다고 보여준다.
- **약한 출처 수십 개 나열 금지** — 관련성 높은 것 먼저, 적게. 많이 대면 신뢰가 희석된다.
- **검증은 화면을 떠나지 않고** — 확인하러 나갔다 돌아오게 만들면 아무도 확인하지 않는다.

### ⚠ 이 축에서 우리가 **채택하지 않는** 것: 신뢰도·확신도 지표
리서치의 상당 부분은 신뢰도 표시(낮음/보통/높음, 근거 충족도 배지)를 권한다. **RAG 기반 사실 응답에는 맞지만 창작 산출물에는 맞지 않는다.** 초안에는 정답이 없어 "이 원고는 신뢰도 70%"가 의미를 갖지 못하고, 거짓 정밀만 만든다. 우리가 가져올 것은 **"무엇이 들어갔나"(근거)** 뿐이고, **"이게 맞나"(신뢰도)** 는 가져오지 않는다.
→ 단, 사실 응답 성격의 기능(예: 추후 데이터 질의)에는 이 축의 신뢰도 부분을 되살릴 수 있다.

출처: agenticuxpatterns.com/patterns/source-anchoring-grounding · creativealive.com/designing-trust-ai-outputs-ux-patterns-uncertainty · bytelabs.space/blog/ux-patterns-for-ai-explainability-and-trust · datawizards.cloud/designing-explainable-overviews · visible-language.org Issue 59-2(Armstrong et al.) · uxpatternsguide.com/patterns/confidence-uncertainty-display

---

## N. 긴 대기(LLM 지연) 상태 ★ (생성 기능 전용)
G축(4상태)의 "200ms 후 스켈레톤"으로는 **수십 초 걸리는 LLM 호출**이 덮이지 않는다. 별도 축으로 다룬다.

- **단계에 이름을 붙인다** — 정체 불명의 스피너보다 "지금 무엇을 하는 중"이 훨씬 안심을 준다. Perplexity "Searching the web…", Claude "Reading document…"가 이 패턴. 퍼센트를 못 보여주면 **순서(3단계 중 2단계)나 완료된 작업 목록**이라도 보여준다.
- **침묵은 신뢰를 깎는다** — 진행 신호 없이 기다리게 하면, 결국 도착한 결과에 대한 확신도 **더 낮아진다**. 대기의 품질이 결과의 신뢰로 이어진다.
- **결과 모양 스켈레톤** — 최종 레이아웃과 같은 형태로. 구조가 먼저 나타나는 것만으로 심리적 안정을 준다.
- **취소 버튼** — 원하지 않게 된 생성을 멈출 수 있게.
- **부분 결과 보존** — 에러·타임아웃·취소 시 이미 나온 것을 지우지 않는다.
- **접근성** — 진행 상태를 polite live region으로 읽어주고, `prefers-reduced-motion`에선 정적 표현 제공.
- **12초 임계** — 웹 응답이 12초를 넘으면 만족도가 급락하고 이탈이 생긴다(Hoxmeier & DiCesare 2000). 넘길 수밖에 없다면 **넘긴다는 사실을 먼저 알린다**(예상 소요 안내).

### 스트리밍은 언제 쓰고 언제 안 쓰나
- **쓴다**: 산출물이 긴 산문이고 부분 출력이 그대로 읽을 가치가 있을 때.
- **안 쓴다**: 산출물이 **구조화 데이터**(배열·필드)라 검증 전 부분 출력으로 아무것도 못 할 때. 이 경우 진행 표시 + 검증된 완성 결과가 낫다.
- 현재 `callLLM`은 스트리밍 미지원 → 도입은 별도 결정 사항.

### 우리 현황 (위반)
`BriefingSection.tsx` 브리핑 생성은 **버튼 라벨을 '생성 중…'으로 바꾸는 것뿐**이다. 이 축 기준 미달이며, 생성 기능 작업 시 함께 개선 후보로 둔다.

출처: ai-tldr.dev/learn/building-ai-apps/ai-ux-patterns/designing-for-llm-latency · ai-tldr.dev/…/optimistic-ui-ai-apps · frontendpatterns.dev/thinking-indicator · uxdesign.cc/the-waiting-problem-in-ai-products · Myers 1985 CHI(percent-done 지표) · Hoxmeier & DiCesare 2000(12초 임계) · arxiv.org/html/2507.22352

---

## O. 생성물 편집·재생성 흐름 ★ (생성 기능 전용)
사용자의 실제 역할은 "생성 버튼을 누르는 사람"이 아니라 **큐레이터·에디터**다. 전량 채택이 아니라 골라 쓰고 고친다. (Wordcraft 전문 작가 워크숍 관찰)

- **수정·거절 경로는 1클릭** — 세 번 눌러야 고칠 수 있으면 아무도 고치지 않고, 나쁜 산출물이 그대로 나간다.
- **부분 재생성을 허용** — 일부가 마음에 안 들 때 전체를 다시 뽑게 강요하지 않는다(비용·시간 낭비이자, 좋았던 부분을 잃는다).
- **원본과 편집본을 함께 보존** — "무엇을 고쳤는가"는 한번 잃으면 복구할 수 없다. 이 계열 제품의 공통 약점이 *"사용자의 수정이 시스템을 학습시키지 않는다"* 인데, 최소한 **기록은 남겨두면** 나중에 반영할 재료가 된다.
- **수락 / 편집 / 폐기를 기록** — 어디서 신뢰가 깨지는지 드러나는 유일한 신호.
- **이전 산출물을 지우지 않는다** — 다시 생성했다고 앞의 것을 없애면, 더 나빠졌을 때 돌아갈 곳이 없다.

### ⚠ 이 축에서 우리가 **채택하지 않는** 것: 인라인 제안 수락(회색→검정)
CoAuthor·Wordcraft 계열 연구의 핵심 패턴은 **글을 쓰는 중에 AI가 다음 문장을 제안하고 Tab으로 수락**하는 것(제안은 회색, 수락하면 검정)이다. 이건 **사람이 먼저 쓰고 AI가 이어붙이는 공동 집필 편집기**의 패턴이고, **초안 전체를 한 번에 생성한 뒤 사람이 고치는** 우리 흐름과 다르다. 회색/검정 구분도 우리에겐 화면 표시가 아니라 **저장 차원(원본 vs 편집본)** 문제로 옮겨온다.
→ 단, "조용한 삽입 금지 / 명시적 수락" 원칙 자체는 유효하다. 사람이 손대지 않은 AI 문장이 최종본으로 **자동 확정되지 않게** 한다.

출처: Wordcraft(arxiv 2107.07430) · CoAuthor(arxiv 2201.06796) · "Who Owns the Text?"(arxiv 2601.10236) · "Co-Writing with AI, on Human Terms"(arxiv 2504.12488) · Hwang, "It was 80% me, 20% AI"(CSCW) · wordcraft-writers-workshop 전문 작가 리포트

---

## P. 위험 표현 인라인 표식 ★ (규제 있는 콘텐츠)
규제·정책 위험이 있는 문구를 **작성자가 판단할 수 있게** 표시한다. 차단이 아니라 표식.

- **문장 전체가 아니라 정확한 구절만** 하이라이트 — 무엇을 고쳐야 할지 정확히 보이게. 문단째 칠하면 정보가 없다.
- **표식마다 ①무엇 ②왜(규정 근거) ③심각도** — 대안이 있으면 ④대안. "왜"가 없으면 학습이 안 되고 매번 다시 묻는다.
- **대안 없는 "인지용 표식"도 정당하다** — 이 경우 선택지는 해결 처리 / 무시뿐. 억지 대안을 만들지 않는다.
- **무시·해결 처리 가능** — 오탐은 반드시 생긴다. 무시할 길이 없으면 표식 전체가 노이즈가 된다.
- **심각도는 색만으로 전달하지 않는다** — 아이콘·텍스트 병기(I축).
- **빨강 남용 금지 — "불안이 놓침보다 나쁘다."** 차분한 톤(앰버 등)으로. 겁주는 화면은 결국 무시된다.
- **고지는 앰비언트로, 모달로 하지 않는다** — "AI 생성물 — 사용 전 확인" 같은 문구는 은은한 푸터가 모달보다 실제로 읽힌다.
- **차단하지 않는다** — 표식은 담당자 확인용이며 법률 자문이 아니다. (`complianceFlags.ts` 주석에 이미 선 원칙)

출처: verbapulse.com · caimpli.com · nowg.net/features/compliance-guardian · docs.markup.ai/apps/figma(인지용 표식·해결/무시) · support.writer.com/article/249-using-suggestions(Approved/Don't use/Use carefully 3분류) · support.grammarly.com(style rules: 원문·대체·설명 3부)

---

## 축 적용 범위 (전부 기계적으로 적용하지 말 것)
축이 16개가 됐다. **모든 화면에 다 적용하는 게 아니라 화면 성격에 맞는 것만 고른다.**

| 화면 성격 | 반드시 볼 축 | 무관한 축 |
|---|---|---|
| 데이터 대시보드 (`/usage`, 추이·주제 분석) | E(대시보드·차트), D(위계), G(4상태) | M·N·O·P |
| 수집·탐색 (덱, 보관함) | A·L(즉시 캡처), G, K | E(차트 없으면), M·O |
| LLM 생성 (브리핑, 콘텐츠 생성) | **M·N·O**, G, H, K | E(차트 없으면) |
| 규제 콘텐츠 산출 (콘텐츠 생성) | **P** + 위 생성 축 전부 | E |
| 전 화면 공통 | B(Nielsen 10), C(인지부하), F(Norman), I(접근성), J(토큰) | — |

---

## 우리 AGENTS.md UX 원칙과의 매핑 (이미 지키는 것)
| AGENTS.md 원칙 | 대응 확장 축 |
|---|---|
| 1. 라벨은 이득을 사용자 언어로 | B-2, H(마이크로카피), K |
| 2. 행동 전 기대 설정(도움말) | H(헬퍼), B-1, G(빈 상태) |
| 3. 숫자 말고 판단까지 서술 | H(결과 서술), E(비교 맥락) |
| 4. 라벨-값 일치(로직 수정) | F(개념모델·거짓 어포던스), B-4 |
| 5. 전문값은 맥락으로 감싸기 | H(플레인랭귀지), I(색+텍스트) |
| 6. 비용 액션 opt-in, 마찰 크면 자동화 | B-5(오류예방), C(Hick), G(낙관적 UI), L(캡처 마찰 제거), N(대기 상태·취소) |

→ 확장 축은 이 원칙들을 **더 넓은 표준(게슈탈트·Nielsen·대시보드·접근성·캡처흐름)으로 뒷받침**한다.
→ 특히 **L(즉시 캡처·인라인·입력 마이크로인터랙션)** 은 "판단이 선 순간 흔적을 남기는" 흐름의 마찰을 없애는 축으로, 원칙 2(기대 설정)·6(마찰 최소화)을 행동 지점에서 구체화한다.
→ **M(근거 표시)** 는 원칙 3(숫자 말고 판단까지 서술)의 생성 기능판이다. 원고만 던지지 않고 "무엇이 반영돼 이렇게 나왔는지"를 함께 보여줘, 담당자가 결과를 통제·검증할 수 있게 한다. 요소별 반영 토글은 **M을 화면에서 완성해야 비로소 통제 장치**가 된다 — 껐다 켠 차이가 보이지 않으면 토글은 거짓 어포던스(F)다.
→ **P(위험 표식)** 는 원칙 5(전문값을 맥락으로 감싸기)의 규제판이다. `効く` 같은 원문을 그대로 노출하되 옆에 "효과 단정 표현(약기법)"을 붙인다.

---

## 다음 단계 (작업 1: 우리 서비스 조준)
이 체크리스트로 아래 화면을 순서대로 진단(Before/After):
1. **`/usage`** — E(대시보드)·D(위계)·G(상태) 중심 (이미 스토리텔링 리팩터 진행 중)
2. **브리핑** — H(카피)·D(위계)·G(에러/빈 상태) + **N(생성 대기 — 현재 버튼 라벨만 바뀌어 미달)**
3. **주제 분석 / 추이** — E(차트-질문 매칭)·A(그룹핑)·C(인지부하)
4. **공통 컴포넌트** — J(토큰·8pt)·I(접근성)·F(어포던스) 전역 적용
5. **콘텐츠 생성 (신규)** — M·N·O·P 전부 + C(Hick)·K(비개발자). 아래 주의 참조

각 화면마다: ①Less is More로 뺄 것 → ②CRAP + 위 축으로 다듬기 → ③4상태·접근성·카피 점검.

### 콘텐츠 생성 화면의 최대 위험 = 새 축이 아니라 C·K축
입력 요소가 6개다(클라이언트, 시술, 레퍼런스 참고 범위, 제작 방향성, 형식, 생성 제약). K축이 경고하는 그림이 정확히 이것 — *"선택지 동시 노출은 초보에게 '어느 게 정답?' 불안을 준다. 가이드가 되어라(게이트키퍼 X)."*

→ **6개를 나란히 늘어놓지 않는다.** 기본값이 이미 채워진 상태로 시작해 담당자는 방향성만 쓰고 바로 생성할 수 있게 하고, 나머지는 접힌 채 "이대로 반영됩니다" 요약만 보여준다. 조절하고 싶을 때만 펼친다. 점진적 공개(K)이자 회상보다 인식(B-6)이다.
