# PayPay QR 이미지 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** PayPay 수취 정보에 QR 이미지를 등록하고, 정산 프로덕트에 전달하고, 정산 쪽이 고치면 우리 명부까지 반영되게 한다.

**Architecture:** 이미지는 새 비공개 버킷 `payment-qr`에 두고 DB에는 **저장소 경로만** 남긴다(절대 URL 금지 — 서명 URL은 만료된다). 내보내기는 RT 증빙(`proof`)과 같은 방식으로 Item에 고정 주소를 싣고 별도 GET 라우트가 바이트를 흘려보낸다. 받기는 새 창구를 만들지 않고 기존 정정 API(`POST …/payment-info`)에 `qr` 키를 더해 멱등·판정·명부 자동 반영(057)을 그대로 재사용한다.

**Tech Stack:** Next.js App Router, TypeScript, postgres.js, Supabase Storage, node:test

## Global Constraints

- 스펙: `docs/superpowers/specs/2026-09-22-paypay-qr-design.md`. 충돌하면 스펙이 우선이다.
- **마이그레이션은 추가만 한다**(`AGENTS.md` "마이그레이션과 배포 순서"). 전 문장 멱등. 트랜잭션 안에서 못 도는 문장 금지.
- 마이그레이션 번호는 **058**. 현재 최신은 `057_payment_correction_roster_sync.sql`.
- 버킷 이름 `payment-qr`. 크기 상한 **5MB(5242880)**. 허용 형식 **`image/jpeg`, `image/png`, `image/webp`**. 마이그레이션의 `file_size_limit`과 코드 상수는 **반드시 같은 값**이어야 한다.
- 저장소 경로 형식: `{influencerId}/{uuid}.{ext}`. **절대 URL을 DB에 넣지 않는다.**
- 옛 QR 파일은 지우지 않는다(이미 나간 요청의 스냅샷이 가리킨다).
- 화면 문구는 `AGENTS.md` UX 원칙을 따른다 — 내부 용어 금지, 이득을 사용자 말로.
- 커밋 메시지는 한국어. 본문에 "왜"를 적는다.
- 린트 기준선은 24개(13 errors, 11 warnings). 이 숫자가 늘면 안 된다.
- 테스트는 연습용 DB에서만 — `npm test`가 `.env.staging`을 쓴다. 운영을 가리키면 `testGuard`가 막는다.

---

## 파일 구조

| 파일 | 책임 |
|---|---|
| `migrations/059_payment_qr.sql` | 버킷 생성·RLS (신규) |
| `src/lib/paymentQr.ts` | QR 상수·검사·업로드·서명·다운로드 (신규) |
| `src/lib/paymentQrInput.ts` | data URI 파싱·검증 — 순수 함수 (신규) |
| `src/lib/influencerPayment.ts` | `PaymentMethod.qr` 타입 + 입력 파싱 (수정) |
| `src/lib/settlementCalc.ts` | `PaymentMethodSnapshot.qr` + `toMethodSnapshot` 키 목록 (수정) |
| `src/lib/settlementPaymentCorrection.ts` | `ALLOWED_BY_TYPE`·`REMOVABLE`·키 목록 3곳 (수정) |
| `src/lib/settlementExternal.ts` | Item에 `qr_url` (수정) |
| `src/app/api/external/settlement/requests/[id]/payment-qr/route.ts` | 이미지 내보내기 (신규) |
| `src/app/api/external/settlement/requests/[id]/payment-info/route.ts` | `qr` base64 → 저장 → 경로 (수정) |
| `src/app/influencers/PaymentQrField.tsx` | 업로드·붙여넣기·미리보기 (신규) |
| `src/app/influencers/PaymentSection.tsx` | 입력 묶음 + 표시 행 (수정) |
| `src/app/settlement/RequestRow.tsx` | 결제수단 줄에 QR 미리보기 (수정) |

---

## Task 1: 버킷과 QR 저장 모듈

**Files:**
- Create: `migrations/059_payment_qr.sql`
- Create: `src/lib/paymentQr.ts`
- Create: `src/lib/paymentQrInput.ts`
- Test: `src/lib/paymentQrInput.test.ts`

**Interfaces:**
- Consumes: 없음 (첫 작업)
- Produces:
  - `PAYMENT_QR_BUCKET = 'payment-qr'`, `MAX_PAYMENT_QR_BYTES = 5242880`, `ALLOWED_PAYMENT_QR_MIME: readonly ['image/jpeg','image/png','image/webp']`
  - `paymentQrValidationError(file: File): string | null`
  - `uploadPaymentQr(influencerId: string, file: File): Promise<string>` — 화면에서 File을 올린다. 저장소 경로 반환
  - `uploadPaymentQrBytes(influencerId: string, bytes: Uint8Array, contentType: string): Promise<string>` — 서버에서 바이트를 올린다(정정 API가 쓴다). 저장소 경로 반환
  - `signPaymentQrUrl(path: string, expiresIn?: number): Promise<string>`
  - `downloadPaymentQrBytes(path: string): Promise<{ body: ReadableStream; contentType: string } | null>`
  - `parseQrDataUri(value: string): { bytes: Uint8Array; contentType: string } | { error: string }`

- [ ] **Step 1: 마이그레이션 작성**

`migrations/059_payment_qr.sql`:

```sql
-- 058: PayPay 수취 QR 이미지 (스펙 2026-09-22-paypay-qr-design.md §1)
-- 칸(컬럼) 추가는 없다 — QR 경로는 influencer.payment_methods(jsonb) 안에 들어간다.
-- 이 파일은 버킷과 접근 정책만 만든다. apply-migrations.sh가 전 파일을 재실행하므로 모든 문장은 멱등.

-- 비공개 버킷. 표시·전달 모두 그때그때 서명 URL로만 접근한다(task-proof 044와 같은 방식).
-- file_size_limit은 src/lib/paymentQr.ts의 MAX_PAYMENT_QR_BYTES와 반드시 같은 값(5MB).
-- task-proof(10MB)보다 좁힌 이유: QR 스크린샷은 보통 수백 KB다.
insert into storage.buckets (id, name, public, file_size_limit, allowed_mime_types)
values ('payment-qr', 'payment-qr', false, 5242880,
        array['image/jpeg','image/png','image/webp'])
on conflict (id) do nothing;

-- on conflict do nothing은 값을 바꾸지 않는다 — 이미 있던 버킷도 코드 상수와 맞춘다.
update storage.buckets
   set file_size_limit = 5242880,
       allowed_mime_types = array['image/jpeg','image/png','image/webp'],
       public = false
 where id = 'payment-qr';

-- create policy에는 if not exists가 없다 — 재실행 안전하게 drop 후 create(024·044 관례).
-- delete 정책을 주지 않는 이유: QR을 바꿔도 옛 파일은 남긴다. 이미 나간 요청의 스냅샷이 그 경로를 가리킨다.
drop policy if exists "payment-qr read" on storage.objects;
create policy "payment-qr read" on storage.objects
  for select to authenticated using (bucket_id = 'payment-qr');

drop policy if exists "payment-qr write" on storage.objects;
create policy "payment-qr write" on storage.objects
  for insert to authenticated with check (bucket_id = 'payment-qr');
```

- [ ] **Step 2: 실패하는 테스트 작성**

`src/lib/paymentQrInput.test.ts`:

```ts
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { parseQrDataUri } from './paymentQrInput.ts';

test('parseQrDataUri — 허용 형식만 통과하고 바이트를 돌려준다', () => {
  // 1x1 png
  const png = 'data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNk+M9QDwADhgGAWjR9awAAAABJRU5ErkJggg==';
  const ok = parseQrDataUri(png);
  assert.ok(!('error' in ok));
  assert.equal(ok.contentType, 'image/png');
  assert.ok(ok.bytes.length > 0);
});

test('parseQrDataUri — 허용 밖 형식은 거절', () => {
  const gif = 'data:image/gif;base64,R0lGODlhAQABAAAAACw=';
  const r = parseQrDataUri(gif);
  assert.ok('error' in r);
  assert.match(r.error, /JPG|PNG|WebP/);
});

test('parseQrDataUri — data URI 형식이 아니면 거절', () => {
  const r = parseQrDataUri('https://example.com/qr.png');
  assert.ok('error' in r);
});

test('parseQrDataUri — base64가 깨졌으면 거절', () => {
  const r = parseQrDataUri('data:image/png;base64,!!!not-base64!!!');
  assert.ok('error' in r);
});

test('parseQrDataUri — 5MB를 넘으면 거절', () => {
  const big = 'data:image/png;base64,' + 'A'.repeat(8 * 1024 * 1024);
  const r = parseQrDataUri(big);
  assert.ok('error' in r);
  assert.match(r.error, /5MB|크기/);
});
```

- [ ] **Step 3: 테스트가 실패하는지 확인**

Run: `node --import tsx --test src/lib/paymentQrInput.test.ts`
Expected: FAIL — `Cannot find module './paymentQrInput.ts'`

- [ ] **Step 4: 순수 파싱 모듈 구현**

`src/lib/paymentQrInput.ts`:

```ts
// 정산 쪽이 정정 API로 보내는 QR을 해석한다(스펙 §3 받기). data URI만 받는다 — 외부 URL을 받으면
// 우리가 남의 서버로 요청을 보내게 되고(SSRF), 그 서버가 죽으면 정정이 실패한다.
// 순수 함수로 둔 이유: 저장소·DB 없이 테스트할 수 있어야 한다.
export const MAX_PAYMENT_QR_BYTES = 5 * 1024 * 1024;
export const ALLOWED_PAYMENT_QR_MIME = ['image/jpeg', 'image/png', 'image/webp'] as const;
export type PaymentQrMime = (typeof ALLOWED_PAYMENT_QR_MIME)[number];

const MIME_LABEL = 'JPG·PNG·WebP';
const DATA_URI_RE = /^data:([a-z]+\/[a-z0-9.+-]+);base64,([A-Za-z0-9+/=\s]+)$/i;

export function parseQrDataUri(value: string): { bytes: Uint8Array; contentType: PaymentQrMime } | { error: string } {
  const m = DATA_URI_RE.exec(value.trim());
  if (!m) return { error: `QR 이미지는 data URI(base64)로 보내 주세요 — 예: data:image/png;base64,…` };
  const mime = m[1].toLowerCase();
  if (!(ALLOWED_PAYMENT_QR_MIME as readonly string[]).includes(mime)) {
    return { error: `QR 이미지는 ${MIME_LABEL}만 돼요 — 받은 형식: ${mime}` };
  }
  // base64는 4글자가 3바이트다 — 디코드 전에 크기를 가늠해 큰 값을 일찍 쳐낸다(메모리 보호).
  const b64 = m[2].replace(/\s+/g, '');
  if ((b64.length * 3) / 4 > MAX_PAYMENT_QR_BYTES) {
    return { error: `QR 이미지는 5MB까지예요` };
  }
  let buf: Buffer;
  try {
    buf = Buffer.from(b64, 'base64');
  } catch {
    return { error: 'QR 이미지를 읽지 못했어요 — base64 값을 확인해 주세요' };
  }
  if (buf.length === 0) return { error: 'QR 이미지가 비어 있어요' };
  if (buf.length > MAX_PAYMENT_QR_BYTES) return { error: `QR 이미지는 5MB까지예요` };
  return { bytes: new Uint8Array(buf), contentType: mime as PaymentQrMime };
}
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `node --import tsx --test src/lib/paymentQrInput.test.ts`
Expected: PASS — 5 tests

- [ ] **Step 6: 저장소 모듈 구현**

`src/lib/paymentQr.ts` — `src/lib/taskProof.ts`를 본뜬다. 먼저 그 파일을 읽고 같은 클라이언트 생성 방식·오류 처리를 따를 것.

```ts
// PayPay 수취 QR 이미지의 저장소 접근(스펙 2026-09-22 §1). task-proof와 같은 구조다.
// 비공개 버킷이라 표시·전달 모두 그때그때 서명 URL을 새로 받는다 — DB에는 경로만 남긴다.
import { ALLOWED_PAYMENT_QR_MIME, MAX_PAYMENT_QR_BYTES } from './paymentQrInput.ts';

export const PAYMENT_QR_BUCKET = 'payment-qr';
export { ALLOWED_PAYMENT_QR_MIME, MAX_PAYMENT_QR_BYTES };

const MIME_EXT: Record<string, string> = { 'image/jpeg': 'jpg', 'image/png': 'png', 'image/webp': 'webp' };

export function paymentQrValidationError(file: File): string | null {
  if (!(ALLOWED_PAYMENT_QR_MIME as readonly string[]).includes(file.type)) return 'QR 이미지는 JPG·PNG·WebP만 돼요';
  if (file.size > MAX_PAYMENT_QR_BYTES) return 'QR 이미지는 5MB까지예요';
  return null;
}

export function paymentQrExtension(contentType: string): string {
  return MIME_EXT[contentType] ?? 'png';
}
```

나머지 네 함수는 `taskProof.ts`의 `uploadTaskProof`·`signTaskProofUrl`·`downloadTaskProof`를 그대로 본뜬다 — **같은 Supabase 클라이언트 획득 방식**을 쓸 것(그 파일을 먼저 읽는다). 바뀌는 건 버킷 이름과 경로 규칙뿐이다.

```ts
function qrPath(influencerId: string, ext: string): string {
  return `${influencerId}/${crypto.randomUUID()}.${ext}`;
}

// 화면에서 고른 파일을 올린다. 경로를 돌려준다(절대 URL이 아니다).
export async function uploadPaymentQr(influencerId: string, file: File): Promise<string> {
  const path = qrPath(influencerId, paymentQrExtension(file.type));
  // supabase.storage.from(PAYMENT_QR_BUCKET).upload(path, file, { contentType: file.type })
  // 오류면 throw — 호출부가 문구를 보여준다
  return path;
}

// 서버가 바이트를 직접 올린다(정정 API). File을 만들 수 없는 자리라 따로 둔다.
export async function uploadPaymentQrBytes(influencerId: string, bytes: Uint8Array, contentType: string): Promise<string> {
  const path = qrPath(influencerId, paymentQrExtension(contentType));
  // supabase.storage.from(PAYMENT_QR_BUCKET).upload(path, bytes, { contentType })
  return path;
}

// 비공개 버킷이라 표시할 때마다 새로 서명한다.
export async function signPaymentQrUrl(path: string, expiresIn = 3600): Promise<string> { /* createSignedUrl */ }

// 그쪽에 흘려보낼 바이트. 파일이 없으면 null(라우트가 404 storage-miss로 바꾼다).
export async function downloadPaymentQrBytes(path: string): Promise<{ body: ReadableStream; contentType: string } | null> { /* download */ }
```

- [ ] **Step 7: 타입 검사·린트**

Run: `npx tsc --noEmit && npx eslint src/lib/paymentQr.ts src/lib/paymentQrInput.ts`
Expected: 오류 없음

- [ ] **Step 8: 연습용 DB에 마이그레이션 적용하고 버킷 확인**

```bash
npm run migrate:staging
```
Then:
```bash
set -a && source .env.staging && set +a && psql -At -c "select id, public, file_size_limit from storage.buckets where id='payment-qr'"
```
Expected: `payment-qr|f|5242880`

- [ ] **Step 9: 커밋**

```bash
git add migrations/059_payment_qr.sql src/lib/paymentQr.ts src/lib/paymentQrInput.ts src/lib/paymentQrInput.test.ts
git commit -m "feat(paypay-qr): QR 이미지 보관함과 입력 해석

PayPay는 수취 식별 정보 대신 QR 스크린샷만 있는 경우가 있다. 이미지를 둘
비공개 버킷(payment-qr)과, 정산 쪽이 보내는 data URI를 해석하는 순수 함수를
먼저 만든다.

외부 URL이 아니라 data URI만 받는다 — 남의 서버로 요청을 보내게 되고(SSRF)
그 서버가 죽으면 정정이 실패한다. 디코드 전에 base64 길이로 크기를 먼저 쳐내
큰 값이 메모리에 올라오지 않게 했다.

버킷 상한 5MB는 코드 상수와 같은 값이어야 한다(044의 관례)."
```

---

## Task 2: 타입에 `qr` 더하기 — 키 목록 네 곳

> **이 작업이 이 계획에서 가장 위험하다.** 선택 항목 키 목록이 네 군데 하드코딩돼 있어, 하나라도 빠뜨리면 QR이 조용히 사라진다. 특히 `mergePaymentMethodCorrection`은 `merged`를 `before`에서 새로 만들기 때문에, 목록에 없으면 **정산 쪽이 수취인 이름만 고쳐도 QR이 날아가고 그 결과가 명부 자동 반영(057)을 타고 원본까지 덮는다.** 스펙 §3-1.

**Files:**
- Modify: `src/lib/influencerPayment.ts` (`PaymentMethod` 타입, `parsePaymentMethodInput`의 paypay 분기)
- Modify: `src/lib/settlementCalc.ts:132-142` (`PaymentMethodSnapshot`, `toMethodSnapshot`)
- Modify: `src/lib/settlementPaymentCorrection.ts` (`ALLOWED_BY_TYPE`, `REMOVABLE`, `TO_CAMEL`, 키 목록 3곳)
- Test: `src/lib/settlementPaymentCorrection.test.ts` (기존 파일에 추가)

**Interfaces:**
- Consumes: Task 1의 상수 없음 (타입만 다룬다)
- Produces:
  - `PaymentMethod.qr?: string`, `PaymentMethodSnapshot.qr?: string`
  - `CorrectionKey`에 `'qr'` 추가 — `ALLOWED_BY_TYPE.paypay = ['holder','identifier','qr']`
  - `REMOVABLE`에 `'qr'` 추가 (null로 지울 수 있다)

- [ ] **Step 1: 실패하는 테스트 작성**

`src/lib/settlementPaymentCorrection.test.ts` 끝에 추가:

```ts
test('mergePaymentMethodCorrection — holder만 고쳐도 기존 qr이 남는다 (스펙 §3-1)', () => {
  const before = { type: 'paypay' as const, holder: '옛 이름', currency: 'JPY' as const,
                   identifier: 'ident-1', qr: 'inf-1/aaa.png' };
  const r = mergePaymentMethodCorrection(before, { holder: '새 이름' });
  assert.ok(r.ok);
  assert.equal(r.after.holder, '새 이름');
  assert.equal(r.after.qr, 'inf-1/aaa.png');       // ← 사라지면 안 된다
  assert.equal(r.after.identifier, 'ident-1');
});

test('mergePaymentMethodCorrection — qr을 바꾸고 지울 수 있다', () => {
  const before = { type: 'paypay' as const, holder: '이름', currency: 'JPY' as const, qr: 'inf-1/old.png' };
  const changed = mergePaymentMethodCorrection(before, { qr: 'inf-1/new.png' });
  assert.ok(changed.ok);
  assert.equal(changed.after.qr, 'inf-1/new.png');

  const removed = mergePaymentMethodCorrection(before, { qr: null });
  assert.ok(removed.ok);
  assert.equal(removed.after.qr, undefined);
});

test('mergePaymentMethodCorrection — PayPal·계좌 수단에 qr은 거절', () => {
  const paypal = { type: 'paypal' as const, holder: '이름', currency: 'JPY' as const, email: 'a@b.com' };
  const r = mergePaymentMethodCorrection(paypal, { qr: 'x/y.png' });
  assert.ok(!r.ok);
  assert.equal(r.field, 'payment_method.qr');
});

test('mergePaymentMethodCorrection — qr 변경이 활동 기록 fields에 남는다', () => {
  const before = { type: 'paypay' as const, holder: '이름', currency: 'JPY' as const, qr: 'inf-1/old.png' };
  const r = mergePaymentMethodCorrection(before, { qr: 'inf-1/new.png' });
  assert.ok(r.ok);
  assert.ok(r.fields.some((f) => f.field === 'qr' && f.to === 'inf-1/new.png'));
});
```

`toMethodSnapshot` 테스트는 `src/lib/settlementCalc.test.ts`에 추가:

```ts
test('toMethodSnapshot — qr 경로를 스냅샷에 싣는다 (스펙 §3-1)', () => {
  const m = { id: 'm1', type: 'paypay' as const, isDefault: true, holder: '이름',
              currency: 'JPY' as const, qr: 'inf-1/aaa.png', updatedAt: '2026-09-22T00:00:00.000Z' };
  assert.equal(toMethodSnapshot(m).qr, 'inf-1/aaa.png');
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `node --import tsx --test src/lib/settlementPaymentCorrection.test.ts src/lib/settlementCalc.test.ts`
Expected: FAIL — 타입 오류 또는 `undefined`

- [ ] **Step 3: 타입에 `qr` 추가**

`src/lib/influencerPayment.ts`의 `PaymentMethod`에 `identifier` 바로 아래:

```ts
  qr?: string;                  // paypay QR 이미지의 저장소 경로 — 선택. 절대 URL이 아니다(059)
```

`parsePaymentMethodInput`의 paypay 분기(`:110`)를 고친다:

```ts
  } else if (type === 'paypay') {
    const identifier = trimmedString(obj.identifier);
    if (identifier) out.identifier = identifier;   // 선택
    const qr = trimmedString(obj.qr);
    if (qr) out.qr = qr;                            // 선택 — 식별 정보와 둘 다 없어도 된다(koo 결정 1)
  } else {
```

`src/lib/settlementCalc.ts:132-142`:

```ts
export interface PaymentMethodSnapshot {
  type: PaymentMethod['type']; holder: string; currency: Currency;
  email?: string; paypalId?: string; identifier?: string; bank?: string; branch?: string; account?: string;
  qr?: string;
}
export function toMethodSnapshot(m: PaymentMethod): PaymentMethodSnapshot {
  const s: PaymentMethodSnapshot = { type: m.type, holder: m.holder, currency: m.currency };
  for (const k of ['email', 'paypalId', 'identifier', 'bank', 'branch', 'account', 'qr'] as const) {
    if (m[k]) s[k] = m[k];
  }
  return s;
}
```

- [ ] **Step 4: 정정 모듈의 키 목록 세 곳 고치기**

`src/lib/settlementPaymentCorrection.ts`:

```ts
const ALLOWED_BY_TYPE: Record<PaymentMethodType, readonly CorrectionKey[]> = {
  paypal: ['holder', 'email', 'paypal_id'],
  paypay: ['holder', 'identifier', 'qr'],
  bank: ['holder', 'bank', 'branch', 'account'],
};
// qr을 REMOVABLE에 넣는 이유: 잘못 올린 QR을 되돌릴 길이 필요하다.
const REMOVABLE: ReadonlySet<CorrectionKey> = new Set(['branch', 'email', 'paypal_id', 'qr']);
```

`TO_CAMEL`에 `qr: 'qr'` 추가. `CorrectionKey` 타입에 `'qr'` 추가.

`mergePaymentMethodCorrection` 안의 키 목록 **두 곳**(`:98`, `:108`)과 `fields` 비교(`:110`)에 `'qr'`을 더한다:

```ts
  for (const k of ['email', 'paypalId', 'identifier', 'bank', 'branch', 'account', 'qr'] as const) if (before[k]) merged[k] = before[k];
  ...
  for (const k of ['email', 'paypalId', 'identifier', 'bank', 'branch', 'account', 'qr'] as const) if (parsed[k]) after[k] = parsed[k];
  ...
  for (const k of ['holder', 'email', 'paypalId', 'identifier', 'bank', 'branch', 'account', 'qr'] as const) {
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `node --import tsx --test src/lib/settlementPaymentCorrection.test.ts src/lib/settlementCalc.test.ts src/lib/influencerPayment.test.ts`
Expected: PASS — 기존 테스트도 전부 통과

- [ ] **Step 6: 타입 검사·린트**

Run: `npx tsc --noEmit && npx eslint src/lib`
Expected: 오류 없음, 린트 24개 유지

- [ ] **Step 7: 커밋**

```bash
git add src/lib/influencerPayment.ts src/lib/settlementCalc.ts src/lib/settlementPaymentCorrection.ts src/lib/settlementPaymentCorrection.test.ts src/lib/settlementCalc.test.ts
git commit -m "feat(paypay-qr): 수단 타입에 qr을 더하고 키 목록 네 곳을 맞춘다

선택 항목 키 목록이 toMethodSnapshot, mergePaymentMethodCorrection 앞뒤,
fields 비교 네 군데에 똑같이 하드코딩돼 있다. 하나라도 빠뜨리면 그 지점에서
QR이 조용히 사라진다.

특히 mergePaymentMethodCorrection은 merged를 before에서 새로 만들기 때문에,
목록에 없으면 정산 쪽이 수취인 이름만 고쳐도 QR이 날아가고 그 결과가 명부
자동 반영(057)을 타고 원본까지 덮는다. 09-22에 fee·memo가 같은 이유로 문제가
될 뻔했다.

'holder만 정정했을 때 qr이 남는다' 테스트가 네 곳을 동시에 지킨다."
```

---

## Task 3: 명부 화면 — 입력과 표시

**Files:**
- Create: `src/app/influencers/PaymentQrField.tsx`
- Modify: `src/app/influencers/PaymentSection.tsx` (draft 상태, paypay 입력 분기 `:362-371`, 표시 행 `:227-231`)

**Interfaces:**
- Consumes: Task 1의 `uploadPaymentQr`·`signPaymentQrUrl`·`paymentQrValidationError`; Task 2의 `PaymentMethod.qr`
- Produces: `PaymentQrField` — props `{ influencerId: string; value: string; disabled?: boolean; onChange: (path: string) => void }`

- [ ] **Step 1: QR 입력 부품 만들기**

`src/components/TaskProofField.tsx`를 먼저 읽는다. 붙여넣기(`:65-74`)와 업로드 흐름을 그대로 본뜬다. 다른 점은 `taskId` 대신 `influencerId`를 쓰고 버킷이 `payment-qr`이라는 것뿐이다.

`src/app/influencers/PaymentQrField.tsx`:
- 파일 선택 버튼 + 드롭 + `paste` 이벤트로 이미지 받기
- `paymentQrValidationError(file)`로 먼저 검사하고, 통과하면 `uploadPaymentQr`
- 값이 있으면 `signPaymentQrUrl`로 서명해 작은 미리보기(높이 약 72px)와 `[바꾸기] [지우기]`
- 누르면 `ImageLightbox`로 확대 (`src/components/ImageLightbox.tsx`, `urls={[signed]} index={0}`)
- 업로드 중에는 버튼 비활성 + "올리는 중" 표시

- [ ] **Step 2: 입력 화면을 묶음으로 바꾸기**

`PaymentSection.tsx:362-371`의 paypay 분기를 교체한다. 지금 안내문("PayPay는 아직 무엇으로 받는지 확정되지 않았어요")은 **지운다** — QR이 생기면 사실이 아니다.

```tsx
{draft.type === 'paypay' && (
  <div className={SPAN2}>
    <p className={FIELD_LABEL}>받을 정보 <span className="text-x-muted font-normal">(둘 중 하나만 있어도 돼요)</span></p>
    <div className="mt-1 space-y-3 rounded-lg border border-x-border p-3">
      <div>
        <label className={FIELD_LABEL} htmlFor={`${uid}-identifier`}>수취 식별 정보</label>
        <input id={`${uid}-identifier`} value={draft.identifier} disabled={busy}
               onChange={(e) => set('identifier', e.target.value)} className={FIELD} />
      </div>
      <div>
        <p className={FIELD_LABEL}>QR 이미지</p>
        <PaymentQrField influencerId={influencerId} value={draft.qr} disabled={busy}
                        onChange={(path) => set('qr', path)} />
      </div>
    </div>
    <p className="mt-0.5 text-caption text-x-muted">
      아직 못 받았으면 비워두셔도 돼요 — 정산 쪽에서 확인되면 그때 채우면 됩니다.
    </p>
  </div>
)}
```

`draft` 상태(`:27`, `:42`, `:68`)에 `qr: string`을 더한다 — `qr: m?.qr ?? ''`, 저장 시 `qr: d.qr`.

`influencerId`가 이 컴포넌트에 없으면 props로 내려받는다.

- [ ] **Step 3: 표시 행 더하기**

`PaymentSection.tsx:227-231`의 paypay 분기:

```tsx
} else if (m.type === 'paypay') {
  rows.push(m.identifier
    ? { key: 'identifier', label: '수취 식별 정보', value: m.identifier, copy: m.identifier }
    : { key: 'identifier', label: '수취 식별 정보', value: '미입력 — 정산 쪽에서 확인되면 적어 두세요', muted: true });
  // QR은 있을 때만 행을 만든다 — 둘 다 선택이라 둘 다 "미입력"이 뜨면 잔소리가 된다(스펙 §2)
  if (m.qr) rows.push({ key: 'qr', label: 'QR 이미지', qrPath: m.qr });
}
```

`Row` 타입에 `qrPath?: string`를 더하고, 행 렌더러가 `qrPath`면 서명 URL로 미리보기를 그리게 한다.

- [ ] **Step 4: 타입 검사·린트**

Run: `npx tsc --noEmit && npx eslint src/app/influencers`
Expected: 오류 없음

- [ ] **Step 5: 화면 확인**

```bash
npm run build && npm start -p 3001
```
`127.0.0.1:3001`에서 인플루언서 한 명 열기 → 결제 수단 추가 → PayPay 선택.
확인: 묶음이 보이는가 / 이미지 올리면 미리보기가 뜨는가 / 붙여넣기가 되는가 / 저장 후 목록에 QR 행이 보이는가 / 눌러서 확대되는가.

> `npm run dev`를 쓰지 말 것 — 하이드레이션이 조용히 실패한다. `localhost` 대신 `127.0.0.1`(크롬 프록시).

- [ ] **Step 6: 커밋**

```bash
git add src/app/influencers/PaymentQrField.tsx src/app/influencers/PaymentSection.tsx
git commit -m "feat(paypay-qr): 명부에서 QR 이미지를 등록하고 본다

PayPay 입력이 '수취 식별 정보' 한 칸뿐이라 QR만 있는 인플루언서를 담을 데가
없었다. 두 칸을 한 묶음으로 묶고 '둘 중 하나만 있어도 돼요'를 위에 적어,
무엇을 넣어야 하는지 그 자리에서 알 수 있게 했다(UX 원칙 2).

'PayPay는 아직 무엇으로 받는지 확정되지 않았어요' 안내는 지웠다 — QR이
생기면 사실이 아니다.

붙여넣기를 지원한다. PayPay QR은 대개 폰 스크린샷이라 파일로 저장했다 올리는
것보다 복사·붙여넣기가 빠르다(RT 증빙과 같은 방식).

QR 행은 있을 때만 만든다 — 둘 다 선택이라 둘 다 '미입력'이 뜨면 잔소리가 된다."
```

---

## Task 4: 정산 프로덕트로 내보내기

**Files:**
- Modify: `src/lib/settlementExternal.ts:58,96` (Item `payment_method`에 `qr_url`)
- Create: `src/app/api/external/settlement/requests/[id]/payment-qr/route.ts`
- Test: `src/lib/settlementExternal.test.ts` (기존 파일에 추가)

**Interfaces:**
- Consumes: Task 2의 `PaymentMethodSnapshot.qr`; Task 1의 `downloadPaymentQrBytes`
- Produces: `GET /api/external/settlement/requests/{id}/payment-qr`

- [ ] **Step 1: 실패하는 테스트 작성**

`src/lib/settlementExternal.test.ts`에 추가:

```ts
test('toExternalItem — QR이 있으면 payment_method.qr_url에 고정 주소를 싣는다', () => {
  const row = makeRow({ paymentMethod: { type: 'paypay', holder: '이름', currency: 'JPY', qr: 'inf-1/a.png' } });
  const it = toExternalItem(row, 'https://deck.example.com');
  assert.equal(it.payment_method.qr_url, `https://deck.example.com/api/external/settlement/requests/${row.row.id}/payment-qr`);
  assert.equal(it.payment_method.qr, undefined);   // 저장소 경로는 내보내지 않는다
});

test('toExternalItem — QR이 없으면 qr_url 키가 없다', () => {
  const row = makeRow({ paymentMethod: { type: 'paypay', holder: '이름', currency: 'JPY' } });
  assert.equal(toExternalItem(row, 'https://deck.example.com').payment_method.qr_url, undefined);
});
```

`makeRow`는 그 파일의 기존 헬퍼를 쓴다. 없으면 기존 테스트가 행을 만드는 방식을 그대로 따른다.

- [ ] **Step 2: 실패 확인**

Run: `node --import tsx --test src/lib/settlementExternal.test.ts`
Expected: FAIL

- [ ] **Step 3: Item에 `qr_url` 넣기**

`settlementExternal.ts`의 `payment_method` 만드는 자리(`:96` 부근). **저장소 경로(`qr`)는 빼고 `qr_url`로 바꾼다** — 경로는 우리 내부 값이라 밖에 나갈 이유가 없다.

```ts
// QR은 경로 대신 고정 주소를 싣는다 — 서명 URL은 만료되므로 목록을 캐시해 두면 죽은 링크가 된다(§4-1과 같은 판단).
const pmOut = { ...pm };
if (pmOut.qr) {
  delete pmOut.qr;
  pmOut.qr_url = `${origin}/api/external/settlement/requests/${r.id}/payment-qr`;
}
```

`SNAKE_PM`에 `qr`이 들어가지 않도록 주의한다(경로를 그대로 내보내면 안 된다).

- [ ] **Step 4: 이미지 라우트 만들기**

`src/app/api/external/settlement/requests/[id]/proof/route.ts`를 읽고 그대로 본뜬다. 바꾸는 것은 셋뿐이다: 읽는 값이 `row.proof`가 아니라 `row.row.paymentMethod.qr`, 다운로드 함수가 `downloadPaymentQrBytes`, `detail`이 `no-qr`·`storage-miss`.

- [ ] **Step 5: 테스트 통과 + 타입·린트**

Run: `node --import tsx --test src/lib/settlementExternal.test.ts && npx tsc --noEmit && npx eslint src`
Expected: PASS, 오류 없음

- [ ] **Step 6: 커밋**

```bash
git add src/lib/settlementExternal.ts src/lib/settlementExternal.test.ts "src/app/api/external/settlement/requests/[id]/payment-qr/route.ts"
git commit -m "feat(paypay-qr): 정산 프로덕트가 QR을 받아갈 수 있게 한다

Item의 payment_method에 qr_url(고정 주소)을 싣고, 그 주소로 GET하면 우리가
그때 서명해서 이미지를 흘려보낸다. 서명 URL을 Item에 직접 넣지 않는 이유는
증빙(§4-1)과 같다 — 만료되므로 목록을 캐시해 두면 죽은 링크가 된다.

저장소 경로(qr)는 내보내지 않는다. 우리 내부 값이라 밖에 나갈 이유가 없다."
```

---

## Task 5: 정산 쪽이 QR을 고칠 수 있게

**Files:**
- Modify: `src/app/api/external/settlement/requests/[id]/payment-info/route.ts`
- Test: `src/app/api/external/settlement/requests/[id]/payment-info/route.test.ts`, `src/lib/settlementStore.test.ts`

**Interfaces:**
- Consumes: Task 1의 `parseQrDataUri`·`uploadPaymentQrBytes`; Task 2의 `ALLOWED_BY_TYPE.paypay`
- Produces: 없음 (마지막 연동 지점)

- [ ] **Step 1: 실패하는 테스트 작성**

`src/lib/settlementStore.test.ts`에 추가 (DB 연동):

```ts
// CID(1)·CID(2)는 앞 테스트가 이미 썼다 — correction_id는 DB 전체에서 한 번만 쓸 수 있으므로
// 저장에 성공하는 테스트는 자기 번호를 써야 한다(파일 상단 CID 주석 참고). 여기는 10번대를 쓴다.
test('applyPaymentMethodCorrection — QR 정정이 요청과 명부에 함께 반영된다', () => revisionOn(async () => {
  const { row } = await requestFor('qr1', 'qr1', { type: 'paypay', qr: 'seed/old.png' });
  const r = await applyPaymentMethodCorrection(sql, row.id, corr({ qr: 'seed/new.png' }, { correctionId: CID(10) }));
  assert.ok(r !== 'not-found' && r.kind === 'applied' && r.rosterApplied === true);

  const after = (await listRequests(sql, { taskId: row.taskId! }))[0];
  assert.equal(after.paymentMethod.qr, 'seed/new.png');
  assert.equal(after.revision, 0);                       // 정정은 판을 올리지 않는다

  const inf = await sql<Array<{ payment_methods: Array<Record<string, unknown>> }>>`
    select payment_methods from influencer where id = ${row.influencerId}`;
  const pm = inf[0].payment_methods.find((m) => m.type === 'paypay')!;
  assert.equal(pm.qr, 'seed/new.png');                   // 명부에도 반영
  assert.equal(pm.isDefault, true);                      // 명부에만 있는 값은 보존
}));

test('applyPaymentMethodCorrection — holder만 고쳐도 QR이 남는다 (스펙 §3-1)', () => revisionOn(async () => {
  const { row } = await requestFor('qr2', 'qr2', { type: 'paypay', qr: 'seed/keep.png' });
  const r = await applyPaymentMethodCorrection(sql, row.id, corr({ holder: '새 이름' }, { correctionId: CID(11) }));
  assert.ok(r !== 'not-found' && r.kind === 'applied');

  const after = (await listRequests(sql, { taskId: row.taskId! }))[0];
  assert.equal(after.paymentMethod.holder, '새 이름');
  assert.equal(after.paymentMethod.qr, 'seed/keep.png');  // ← 네 곳 중 하나라도 빠지면 여기서 undefined
}));

test('applyPaymentMethodCorrection — qr을 null로 지운다', () => revisionOn(async () => {
  const { row } = await requestFor('qr3', 'qr3', { type: 'paypay', qr: 'seed/gone.png' });
  const r = await applyPaymentMethodCorrection(sql, row.id, corr({ qr: null }, { correctionId: CID(12) }));
  assert.ok(r !== 'not-found' && r.kind === 'applied');
  assert.equal((await listRequests(sql, { taskId: row.taskId! }))[0].paymentMethod.qr, undefined);
}));
```

`requestFor`는 이 파일의 기존 헬퍼다. PayPay 수단과 QR을 심을 수 있게 **세 번째 인자를 받도록 넓혀야 한다** — 지금은 기본 수단으로만 만든다. 기존 호출부는 인자를 안 주면 지금과 같게 동작해야 한다(기존 테스트가 깨지면 안 된다).

- [ ] **Step 2: 실패 확인**

Run: `node --import tsx --env-file-if-exists=.env.staging --import ./scripts/testGuard.ts --test src/lib/settlementStore.test.ts`
Expected: FAIL

- [ ] **Step 3: 라우트에서 QR을 먼저 저장하기**

`payment-info/route.ts`의 본문 해석 뒤, **`applyPaymentMethodCorrection`을 부르기 전에** 처리한다.

```ts
// qr만 특별 취급한다 — 받은 base64를 저장소에 넣고, 이후 처리에는 경로만 쓴다.
// 그래야 정정 이력(patch·before·after)에 base64가 아니라 경로가 남는다(기록 테이블이 붓지 않게).
// 저장을 DB 트랜잭션보다 먼저 하는 이유: 트랜잭션 안에서 외부 저장소를 만지면 실패 시 롤백이 어긋난다.
// 저장은 됐는데 DB가 롤백되면 고아 파일이 남지만 비공개 버킷이라 무해하다 — 반대가 위험하다.
const qrRaw = parsed.correction.patch.qr;
if (typeof qrRaw === 'string' && qrRaw.length > 0) {
  const decoded = parseQrDataUri(qrRaw);
  if ('error' in decoded) {
    recordExternalCallSafe({ ...c, requestId: id, statusCode: 400, outcome: 'bad-request', detail: 'payment_method.qr', body: raw });
    return NextResponse.json({ success: false, error: decoded.error, field: 'payment_method.qr' }, { status: 400, headers: NO_STORE });
  }
  const influencerId = await influencerIdOfRequest(sql, id);   // 없으면 404 (아래 판정이 다시 잡는다)
  if (influencerId) {
    parsed.correction.patch.qr = await uploadPaymentQrBytes(influencerId, decoded.bytes, decoded.contentType);
  }
}
```

`influencerIdOfRequest`가 없으면 `settlementStore.ts`에 작은 조회 함수를 더한다:

```ts
export async function influencerIdOfRequest(sql: postgres.Sql, id: string): Promise<string | null> {
  if (!isUuidLike(id)) return null;
  const r = await sql<Array<{ influencer_id: string }>>`select influencer_id from payment_request where id = ${id}`;
  return r[0]?.influencer_id ?? null;
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `node --import tsx --env-file-if-exists=.env.staging --import ./scripts/testGuard.ts --test src/lib/settlementStore.test.ts "src/app/api/external/settlement/requests/[id]/payment-info/route.test.ts"`
Expected: PASS

- [ ] **Step 5: 타입·린트**

Run: `npx tsc --noEmit && npx eslint src`
Expected: 오류 없음, 린트 24개

- [ ] **Step 6: 커밋**

```bash
git add src/app/api/external/settlement/requests src/lib/settlementStore.ts src/lib/settlementStore.test.ts
git commit -m "feat(paypay-qr): 정산 쪽이 QR을 고치면 요청과 명부에 반영된다

새 창구를 만들지 않고 9/21에 만든 정정 API에 qr 키를 더했다. 멱등 처리,
취소·지급완료·판 불일치 판정, 명부 자동 반영(057), 활동 기록이 전부
그대로 재사용된다.

qr만 특별 취급한다 — 받은 base64를 저장소에 먼저 넣고 이후 처리에는 경로만
쓴다. 그래야 정정 이력에 base64가 아니라 경로가 남아 기록 테이블이 붓지 않는다.

저장을 DB 트랜잭션보다 먼저 하는 이유: 트랜잭션 안에서 외부 저장소를 만지면
실패 시 롤백이 어긋난다. 저장은 됐는데 DB가 롤백되면 고아 파일이 남지만
비공개 버킷이라 무해하다 — 반대(DB가 가리키는 파일이 없는 상태)가 위험하다."
```

---

## Task 6: 정산 요청 내역에 QR 미리보기

**Files:**
- Modify: `src/app/settlement/RequestRow.tsx` (결제수단 줄)

**Interfaces:**
- Consumes: Task 1의 `signPaymentQrUrl`; Task 2의 `PaymentMethodSnapshot.qr`
- Produces: 없음

- [ ] **Step 1: 결제수단 줄에 미리보기 더하기**

같은 파일이 증빙에 이미 `ImageLightbox`를 쓰고 있다(`:112`). 같은 방식으로 QR 미리보기를 붙인다. QR이 없으면 아무것도 그리지 않는다.

- [ ] **Step 2: 타입·린트 + 화면 확인**

Run: `npx tsc --noEmit && npx eslint src/app/settlement`
그리고 `npm run build && npm start -p 3001` → `/settlement`에서 PayPay 요청을 펼쳐 QR이 보이는지.

- [ ] **Step 3: 커밋**

```bash
git add src/app/settlement/RequestRow.tsx
git commit -m "feat(paypay-qr): 정산 요청 내역에서 QR을 바로 본다

요청을 보낼 때 무엇이 나갔는지 그 자리에서 확인할 수 있어야 한다. 증빙과
같은 확대 보기를 쓴다."
```

---

## Task 7: 계약 문서·전달 문안·업데이트 소식

**Files:**
- Modify: `docs/api/settlement-external-api.md`
- Create: `docs/api/settlement-handoff/27_우리_PayPay_QR_슬랙_20260922.md`
- Modify: `docs/api/settlement-handoff/README.md`
- Modify: `src/content/updates.ts`

- [ ] **Step 1: 계약 문서 고치기**

- §5 `payment_method` 설명에 `qr_url` 추가 — "PayPay에서 QR로 받는 경우. 이 주소로 GET하면 이미지가 온다. 값이 없으면 키 자체가 없다"
- §4-2로 `GET …/payment-qr` 신설 — §4-1(proof)과 같은 형식으로, 404 세 경우 포함
- §6-1(정정)의 paypay 허용 키에 `qr` 추가 — "data URI(base64)로 보낸다. `null`로 지울 수 있다. 5MB, JPG·PNG·WebP"
- 개정 기록에 한 줄

- [ ] **Step 2: 전달 문안 쓰기**

`docs/api/settlement-handoff/`의 기존 문서 형식을 따른다. 담을 것: 왜 생겼는지(QR만 있는 인플루언서), 받아가는 법(`qr_url`), 고치는 법(정정 API에 `qr` 키), 제한(5MB·형식), 지울 때(`null`). `README.md` 표에 27번 행 추가.

- [ ] **Step 3: 업데이트 소식 쓰기**

`src/content/updates.ts` 맨 위에. 유형은 `새 기능`.

```ts
{
  date: '2026-09-22', type: '새 기능',
  title: 'PayPay는 QR 이미지로도 받을 수 있어요',
  summary: '인플루언서가 PayPay 수취 식별 정보 대신 QR 이미지만 주는 경우가 있어요. 이제 인플루언서 프로필의 결제 수단에 QR 이미지를 등록할 수 있고, 정산 요청을 보낼 때 함께 전달돼요.',
  bullets: [
    '결제 수단에서 PayPay를 고르면 "받을 정보" 묶음이 나와요 — 수취 식별 정보와 QR 이미지 중 하나만 있어도 되고, 아직 못 받았으면 비워두셔도 돼요',
    'QR 이미지는 복사해서 붙여넣어도 올라가요 — 폰 스크린샷을 파일로 저장할 필요가 없어요',
    '등록한 QR은 정산 요청 내역에서도 바로 볼 수 있어요',
    '정산 쪽에서 QR을 바꾸면 요청과 인플루언서 명부에 함께 반영돼요 — 수취인 이름이나 계좌를 고칠 때와 같아요',
  ],
  link: { label: '인플루언서 명부', href: '/influencers' },
},
```

- [ ] **Step 4: 형식 검사**

Run: `node --import tsx --test src/lib/updates.test.ts`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add docs/api src/content/updates.ts
git commit -m "docs(paypay-qr): 계약·전달 문안·업데이트 소식

그쪽이 QR을 받아가고 고치는 방법을 계약에 적고, 슬랙으로 보낼 문안(27)을
따로 둔다. 사용자에게는 '무엇을 할 수 있게 됐는지'로 알린다."
```

---

## 마무리 (모든 Task 후)

- [ ] 전체 테스트: `npm test` — 실패는 `trackingLink` 1건(무작위 코드 생성, 원래 간헐적)만 허용
- [ ] 린트: `npx eslint .` — 24개 유지
- [ ] 연습용 DB에 마이그레이션 적용 상태 확인
- [ ] `docs/superpowers/plans/2026-09-22-paypay-qr.md`의 체크박스 갱신
- [ ] koo에게 화면 확인 요청 (명부 입력·표시, 정산 요청 내역)

**머지 시 주의:** 058은 추가만 하므로 ADR 0007 기준 **머지 1번**이면 된다. 빌드가 마이그레이션을 먼저 적용하므로 운영 DB를 손으로 건드릴 필요가 없다.
